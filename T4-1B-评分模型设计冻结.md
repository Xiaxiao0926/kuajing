# T4-1B 评分模型设计冻结（V1 机器可执行规格）

> 日期：2026-08-14
> 分支：`feat/v3-t4-product-scoring`
> 状态：**模型设计冻结文档。本文件不含任何代码实现；T4-2 起按本规格实现 `scoringEngine.js`。**
> 输入依据：`T4-0-选品数据审计报告.md`（候选 1000×63 字段）+ `T4-1A-BSR市场基准层审计报告.md`（BSR 855 类型/19 域基准 + 映射覆盖率）+ 需求方 2026-08-14 决策。

---

## 1. 数据层（T4-2 实现时的输入契约）

### 1.1 候选商品 canonical 字段（来自 选品/跨境项目产品线扩展计划.xlsx）

| canonical 键 | 原始列 | 清洗规则 |
|---|---|---|
| `sku_id` | 商品ID | 字符串化 |
| `name` | 商品名称 | 原样 |
| `brand` | 品牌 | 空→'未知' |
| `category_leaf` | 所属类目 最后一段 | 中文叶子名 |
| `rating` | 商品评分 | 数值；0=新品未评分（仅影响置信度） |
| `reviews` | 评论数 | 数值 |
| `price_rub` | 价格 | >0 正常；=0 且 avg_price 有效→用 avg_price 并标 `LIST_PRICE_MISSING`；两者无效→price=N/A |
| `avg_price_rub` | 平均单价 | 数值 |
| `sales_rub_28d` | 销售额 | 数值（主需求指标） |
| `units_28d` | 销量 | 数值（主需求指标） |
| `daily_units` | 平均销量 | **仅校验/展示，不参与评分**（28.5 倍关系已验证） |
| `daily_sales_rub` | 每日平均订单金额 | **同上不参与** |
| `exposure` | 曝光量 | 数值 |
| `card_visits` | 浏览次数 | 数值 |
| `conv_rate` | 订单转化率 | 文本%→数值；winsorize |
| `cart_add_rate` | 商品卡加入购物车率 | 文本%→数值 |
| `gross_margin` | 预估毛利率 | 文本%→数值（可为负） |
| `commission_fbs/fbo/rfbs/fbp` | 四佣金列 | 数值% |
| `ad_share` | 广告占比 | 文本%→数值 |
| `weight_g` | 重量 g | 数值 |
| `volume_l` | 体积/公升 | 数值 |
| `dims_l/w/h_cm` | 尺寸三列 | 数值 |
| `ship_mode` | 发货模式 | FBO/FBS/FBOFBS/OZON 四值 |
| `sign_rate` | 签收率 | 数值% |
| `oos_days_share` | 无库存天占比 | 数值% |
| `stock` | 期末库存数 | 数值 |
| `turnover` | 周转动态 | winsorize(1-99) 后转秩 |
| `revenue_loss` | 收入损失 | 数值 |
| `compliance_hit` | 合规关键词命中 | 引擎侧按 HIGH_RISK_KEYWORDS 计算（沿用 app.py 词表），命中→REVIEW_REQUIRED |

**数据铁律（T4-0 §4.3 全量继承）**：
1. 百分比字段 parseFloat + winsorize(1/99 百分位)；
2. 价格=0 → 按 §1.1 回退，**禁止类目中位回填**；
3. 评分=0/评论=0 新品 → 只降"证据置信度"，不扣质量分；
4. 人民币列不采信，需要时从卢布×config 汇率推导；
5. 归一化一律**百分位秩（0-100）**，不用 min-max；
6. `undefined` 列、`从搜索加入购物车率`、`配送时间`、`发现时间` 弃用。

### 1.2 BSR 市场基准（来自 bsr_market_benchmarks.json）

- 匹配层级（**终版，无 global fallback**）：

```text
精确匹配 n≥10        → type benchmark,        Context=HIGH
精确匹配 5≤n<10      → type/domain shrinkage, Context=MEDIUM
精确匹配 n<5         → domain benchmark,      Context=LOW
包含匹配             → domain benchmark,      Context≤MEDIUM（LOW 或 MEDIUM，人工确认前不得 HIGH）
完全未匹配           → NO BSR BENCHMARK,      Context=LOW_MARKET_CONTEXT
```

- **不存在"全局 BSR 基准"这一回退层级**。未匹配时各维度口径：
  - demand → 候选池 percentile；
  - competition / price_opportunity / supplyGap → **N/A**；
  - profitability / logistics / operations → 候选池/自身数据。
- shrinkage 公式（仅 5≤n<10，**写死，禁止 AI 换系数或 Bayesian prior**）：

```text
α = n / (n + 5)
BlendedMetric = α × TypeMetric + (1-α) × DomainMetric
```

  适用于 P25/P50/P75/P90、HHI、Top share、promo/ad 等可线性指标；缺一侧时取有的一侧，两侧都无 → N/A。
- BSR 标签**严格隔离**：不参与评分，仅 T4-5 做 validation enrichment。
- **percentile 比较池定义**：所有"市场结构 percentile"（HHI/Top share/promo/ad 等）统一在**同一个 BSR domain 下的 product types** 中排名；Supply Gap 的 percentile 同此池，且该 domain 内可比较 product type <5 个时 supplyGap=N/A。

---

## 2. 总分公式（证据感知评分，两层）

```text
TotalScore = Σ(可用维度权重 × 维度分) / Σ(可用维度权重)
```

- 维度"不可计算"（N/A）→ 该维度权重从分子分母同时剔除，**其余维度重新归一**；
- 所有维度 N/A → Score=N/A，输出 NEEDS_DATA；
- 输出同时带 `evidence_coverage` = Σ(可用权重)/100，如 75%（缺竞争15+价格10）。

**第二层：子指标同样证据感知（进入 normalization.js 通用逻辑）**：

```text
dimensionScore = Σ(可用子权重 × 子分) / Σ(可用子权重)
子指标有效权重覆盖 ≥ 50% → 该维度 available
子指标有效权重覆盖 < 50% → 该维度 N/A
```

### 2.1 六维权重（冻结）

| 维度 | 权重 | 方向 |
|---|---:|---|
| demand 市场需求 | **25** | 越高越好 |
| competition 竞争机会 | **15** | 越高越易进入 |
| price_opportunity 价格空间 | **10** | 越高越好 |
| profitability 利润可行性 | **20** | 越高越好 |
| logistics 物流适配 | **15** | 越高越好 |
| operations 运营稳健 | **15** | 越高越好 |

### 2.2 子权重（冻结）

**demand（T4-4A 修订：两层结构 = 市场规模 + 候选相对强度）**

```text
DemandScore = λ × MarketScaleScore + (1 − λ) × CandidateStrengthScore
```

- `CandidateStrengthScore` = 原 demand 算法（候选在自身市场/候选池的相对表现，下表 7 项证据感知加权，覆盖 <50% → 维度 N/A）；
- `MarketScaleScore` = `evidence-weighted[(pct(市场 sales_28d.p50 于全局产品类型池), 60%) ⊕ (pct(市场 units_28d.p50 于全局产品类型池), 40%)]`，缺一侧按剩余权重重归一，两侧全缺 → N/A；
- **适用范围**：仅 `HIGH / MEDIUM`（可靠产品类型基准）计算 MarketScale；`LOW`（domain fallback / 包含匹配）与 `LOW_MARKET_CONTEXT` → MarketScale=N/A，DemandScore=候选强度（路径与 T4-2/3 完全一致，771 个 LMC SKU 不受影响）；
- **全局产品类型池只用于市场规模排名，绝不冒充对应市场基准**（T4-1A 禁令不变）；`bsr_leader_share` 禁止进入公式（仅观察标签）；
- λ 经 T4-4A 校准实验冻结：**λ = 0.5**（30/40/50 三组取最小可行值：30 双 REVERSED、40 仅 units 转正、50 双转正；不再上探 60/70，见 `T4-4A-Demand语义校准报告.md`），配置于 `config/scoring_rules.json` → `dimensions.demand.scale_weight`。

| 子项（CandidateStrength） | 权重 | 比较池 |
|---|---:|---|
| sales_28d | 35% | BSR（有映射）或候选池（无映射） |
| units_28d | 25% | 同上 |
| conv_rate | 15% | 同上 |
| cart_add_rate | 10% | 同上 |
| exposure | 5% | **V1 固定候选池 percentile**（BSR benchmark 无该聚合） |
| card_visits | 5% | **V1 固定候选池 percentile** |
| reviews | 5% | **V1 固定候选池 percentile** |

demand 维度输出新增两个可解释分量：`marketScaleScore`、`candidateStrengthScore`（UI 可分开展示"市场规模/候选相对表现"）。

**competition（越高越易进入；无 BSR 映射 → N/A）**

| 子项 | 权重 | 计算 |
|---|---:|---|
| 卖家集中度 | 30% | `100 - seller_hhi_percentile`（同 domain 类型池） |
| 品牌集中度 | 25% | `100 - brand_hhi_percentile`（同 domain 类型池） |
| 头部份额压力 | 15% | `100 - max(top1_brand_share, top10_seller_share)百分位` |
| 促销依赖 | 15% | `PromoDependencyScore = evidence-weighted[(100 − pct(promo_share), 60%) ⊕ (100 − pct(promo_days), 40%)]`。缺一项按剩余权重重归一，两项全缺 → 子项 N/A；**禁止用 0 补齐缺失项** |
| 广告机会 | 15% | `AdOpportunityScore = evidence-weighted[(100 − pct(ad_days), 60%) ⊕ (pct(ad_roi), 40%)]`——**ad_days 越少越好；ad_roi 越高越好**。缺一项按剩余权重重归一，两项全缺 → 子项 N/A；**禁止用固定 50 补齐缺失项** |

样本量只决定回退层级与置信度，**不直接加分**（样本少 ≠ 竞争少）。

**price_opportunity（无 BSR 映射 → N/A）**

| 子项 | 权重 | 计算 |
|---|---:|---|
| 价格带匹配 | 50% | 候选价格落在市场 P25-P75 → 满分；P10-P25 或 P75-P90 → 60%；P10 以下 → 30%（价格带错配风险）；P90 以上 → 40% |
| 价格下限压力 | 25% | **正式冻结**：`clamp(100 × P25(min_price_rub) / P50(avg_price_rub), 0, 100)`。市场底线越接近主流成交中位价，越不易陷入极端低价竞争（越高分越好） |
| 折扣稳定性 | 25% | **正式冻结**：`clamp(100 − P50(discount), 0, 100)`。市场中位折扣 20%→80 分 / 50%→50 分 / 80%→20 分；长期大折扣 = 价格稳定性差 |

价格口径（T4-0 Q5 决策）：`price>0` 用 price；`price=0 且 avg_price>0` 用 avg_price 并标 `LIST_PRICE_MISSING`；都无效 → 维度 N/A。

**字段跨维度使用边界（防重复）**：
- competition 用 `promo_share / promo_days`（促销**频率与依赖**）；
- price 用 `discount`（折扣**深度**）；
- 同一字段不得跨维度重复。

**profitability（Profitability Proxy，无真实采购成本）**

| 子项 | 权重 | 计算 |
|---|---:|---|
| gross_margin | 60% | 百分位秩 |
| 佣金负担 | 20% | 发货模式明确→对应佣金；混合→**取可适用佣金最高值**（保守）；`100 - 佣金百分位` |
| 广告负担 | 20% | `100 - ad_share 百分位` |

**Gate**：`gross_margin < 0` → profitability 分封顶 **20**，且 status 加 `MARGIN_RISK`。

**logistics（复用 T2 CEL 17 渠道引擎；货币换算必须写死）**

```text
effectivePriceCny = effectivePriceRub / rubPerCny
shippingRatio     = minValidShippingCny / effectivePriceCny
```

- `rubPerCny` **必须读取 T2 canonical `config/settings.json`**，scoring_rules 不允许维护第二个汇率；
- `effectivePriceCny ≤ 0` → shippingRatio 子项 N/A。

| 子项 | 权重 | 精确公式 |
|---|---:|---|
| 可发渠道 | 40% | `channelCountScore = min(validChannelCount / 5, 1) × 100`（≥1 条有分，≥5 条=100） |
| 运费/成交价 | 35% | shippingRatio 在 1000 候选有效样本中做 percentile → `score = 100 - percentile`。**候选池预计算铁律：尺寸无效的 SKU 不得进入 shipping_ratio / billable_weight 的 percentile 池** |
| 计费重量 | 15% | 候选池 percentile → `score = 100 - percentile`（同池铁律：仅尺寸/重量有效的 SKU 入池） |
| 体积重惩罚 | 10% | 未触发=100；体积重/实重 ≤1.25 → 70；≤1.50 → 40；>1.50 → 0 |

**Gate**：无任何 CEL 渠道可发 → logistics=0 且 status 加 `BLOCKED_LOGISTICS`。**该 Gate 不能被其他维度平均掉。**

**operations（广告/促销已在竞争维度，此处不重复计）**

| 子项 | 权重 | 计算 |
|---|---:|---|
| sign_rate | 35% | 百分位秩 |
| oos_days_share | 25% | `100 - 百分位秩` |
| stock 可用性 | 15% | 有库存→满分；低库存→阶梯 |
| turnover 稳定性 | 15% | winsorize 后 |秩-50| 距离映射 |
| revenue_loss | 10% | **用 `revenue_loss_rate = revenue_loss / sales_rub_28d`**（避免绝对金额错罚大市场）；`sales_rub_28d ≤ 0` 时该子项 N/A |

---

## 3. Market Context 置信度（独立于分数）

| Context | 条件 |
|---|---|
| HIGH | 精确匹配 + n≥10 |
| MEDIUM | 精确匹配 + 5≤n<10（shrinkage） |
| LOW | 包含匹配 或 n<5 回退 domain |
| LOW_MARKET_CONTEXT | 无 BSR 匹配（77.1% 候选） |

包含匹配**未经人工确认最高只能 MEDIUM/LOW**，不得与精确匹配同权。

---

## 4. Supply Gap Signal（独立解释信号，不进总分）

仅在 **Context ≠ LOW_MARKET_CONTEXT** 时计算；否则 N/A（**禁止用全局市场推缺口指数**）。

```text
percentile universe = 同一个 BSR domain 下的 product types
若该 domain 内可比较 product type < 5 个 → supplyGap = N/A

Demand Rank   = sales_p50 在同域产品类型中的百分位
Shortage Rank = 40% × 无库存天数百分位 + 60% × 错失销售额百分位
Supply Pressure = 45% × Demand Rank + 55% × Shortage Rank
Entry Openness  = 100 − Seller HHI Percentile
Gap = Supply Pressure × (0.80 + 0.20 × Entry Openness / 100)
```

阈值：

```text
HIGH_GAP   : Gap ≥ 70 且 Demand ≥ 60 且 Shortage ≥ 65
MEDIUM_GAP : Gap ≥ 55 且 Demand ≥ 50 且 Shortage ≥ 55
否则        : NO_STRONG_GAP_SIGNAL
```

---

## 5. 评级与 Gate/Flag（冻结）

### 5.1 评级

```text
A   80–100     优先
B   65–79.99   值得测试
C   50–64.99   有条件观察
D   <50        暂不优先
null            不可评级
```

**NEEDS_DATA（可用维度权重之和 <50%）→ `grade = null`（不可评级）**：`totalScore` 保留为诊断值，但不得参与 A/B/C/D 评级；五档分布 = A / B / C / D / 不可评级。

Context = LOW_MARKET_CONTEXT 时评级显示为**暂定**（如 "A · 暂定"），推荐动作改为"优先补市场研究"，不自动进入样品阶段。

### 5.2 Gate/Flag 层（独立于综合分，不可被平均掉）

| Flag | 触发 | 效果 |
|---|---|---|
| BLOCKED_LOGISTICS | 无 CEL 可用线路 | logistics=0；decision=BLOCKED |
| REVIEW_REQUIRED | 合规关键词命中 | 提示人工确认（≠不合规） |
| MARGIN_RISK | 预估毛利率<0 | profitability 封顶 20 |
| LOW_MARKET_CONTEXT | 无 BSR 对应市场 | 评级暂定、补市场研究 |
| NEEDS_DATA | 关键字段证据不足（可用维度权重之和 <50%） | 输出不可评级 |

### 5.3 决策层（`decision`，与 `grade` 分离；**不靠篡改总分解决 Gate**）

> `grade` = 机会质量（即使物流 0 分，其余五维 100 时 Total=85 仍是 A——这是正确的市场信息）；
> `decision` = 当前能否进入下一步。

```text
BLOCKED_LOGISTICS
→ decision.status = BLOCKED, action = DO_NOT_SAMPLE

MARGIN_RISK
→ decision.status = HOLD, action = VERIFY_COST

REVIEW_REQUIRED
→ decision.status = REVIEW, action = COMPLIANCE_REVIEW

LOW_MARKET_CONTEXT
→ decision.status = RESEARCH, action = COLLECT_MARKET_DATA

无严重 flag
→ decision.status = ELIGIBLE
→ action 按 A/B/C/D：
   A → SAMPLE_VALIDATION
   B → PILOT_TEST
   C → WATCH
   D → DEPRIORITIZE
```

**黄金案例 Gate 断言口径（修正）**：物流不可发 → **raw grade 可为 A，但 decision.status 必须 = BLOCKED、action = DO_NOT_SAMPLE**，禁止进入样品阶段。

---

## 6. 输出结构（两段式契约）

**第一段：`scoreProduct(candidate, marketContext, deps, rules)` → 数值/状态/decision（不生成文案）**

```javascript
{
  totalScore: 84.2 | null,          // 诊断值；NEEDS_DATA 时仍保留但 grade=null
  grade: 'A' | 'B' | 'C' | 'D' | null,   // null = 不可评级（NEEDS_DATA 或全维度不可用）
  gradeTentative: true | false,
  context: 'HIGH' | 'MEDIUM' | 'LOW' | 'LOW_MARKET_CONTEXT',
  evidenceCoverage: 0.75,          // Σ可用维度权重/100，保留 2 位小数（0.75 不得 round1 成 0.8）
  dimensions: { demand: {score, weight, available, coverage, subs:[{key,score,weight,label}]}, ... },
  supplyGap: { signal: 76, rank: 'HIGH_GAP', demandRank, shortageRank, entryOpenness } | null,
  status: ['MARGIN_RISK', 'LOW_MARKET_CONTEXT', ...],
  decision: { status: 'ELIGIBLE'|'BLOCKED'|'HOLD'|'REVIEW'|'RESEARCH', action: 'SAMPLE_VALIDATION'|... , reason: '...' },
  ruleVersion: '1.0',
  matchedProductType: '汽车芳香剂' | null,        // null=未匹配
  benchmarkSampleSize: 25 | null,
}
```

**第二段：`buildExplanations(candidate, result, marketContext)` → 解释文案（不在评分核心内）**

```javascript
{
  strengths: ['销量超过同类市场 68% 商品', ...],
  risks: ['价格带错配: 市场主成交带 180-260₽, 方案成本需 650₽', ...],
  missingMetrics: ['竞争机会(无市场基准)', ...],
}
```

UI 层组合两段输出展示；`scoreProduct` 永不返回 strengths/risks/missingMetrics。

解释文案口径铁律（context 分级，禁止把 domain 基准伪装成产品类型基准）：
- HIGH / MEDIUM（type 基准）→ 可写"超过同类市场 X 商品"；
- LOW（domain 基准 / shrinkage 混合）→ 只能写"对应 BSR 市场域"；
- LOW_MARKET_CONTEXT（无映射）→ 只能写"候选池表现前 X%"。 

---

## 7. 配置化要求（T4-2 实现）

- 权重/子权重/阈值/评级全部进入 `config/scoring_rules.json`（V1 数值 = 本文件）；
- 引擎纯函数 `scoreProduct(product, marketContext, rules)`，无副作用、无网络；
- 归一化函数独立于引擎（`normalization.js`：百分位秩、winsorize）；
- `percentileRankFromQuantiles` 越界截断到**首/末分位对应百分位**（只有 P10–P90 数据时：低于 P10 → 10、高于 P90 → 90；不得声称 P0/P100）；
- 解释文案生成独立于引擎（`explanations.js`，两段式契约见 §6）；
- 单测 `scoringEngine.test.mjs` 覆盖 §8 场景。

---

## 8. T4-3 黄金评分案例清单（预登记，V1 冻结范围）

| 案例 | 断言方向 |
|---|---|
| 明显优秀 SKU（高需求+高毛利+轻小件+有映射） | grade=A, context∈{HIGH,MEDIUM}, decision=ELIGIBLE |
| 明显差 SKU（低需求+负毛利+重件） | grade≤C, 含 MARGIN_RISK, decision=HOLD |
| 高销量但亏损 | 总分被 MARGIN_RISK 压低, profitability≤20, decision=HOLD |
| 低竞争但无需求（BSR 缺货信号弱） | 无 HIGH_GAP, grade≤C |
| 高利润但物流过重（无 CEL 渠道） | logistics=0, **decision.status=BLOCKED, action=DO_NOT_SAMPLE**（raw grade 可为 A） |
| 数据缺失 SKU（价格=0 且 avg 无效） | price N/A, evidenceCoverage<1 |
| 异常极值 SKU（周转动态 30325%、评论 157 万） | 正常打分不崩溃, 无极端影响 |
| 临界 A/B SKU（80 分附近） | grade 边界正确 |
| 无 BSR 映射 SKU | LOW_MARKET_CONTEXT, 暂定评级, supplyGap=null, decision=RESEARCH |
| 包含匹配 SKU | context≤MEDIUM, 不得 HIGH |
| 子指标覆盖 <50% 的维度 | 该维度 N/A（如 demand 仅 reviews 可用） |

---

## 9. 明确禁止（T4 全阶段，与任务书一致）

- BSR 标签进入评分公式；
- 全局基准伪装对应市场；
- 类目中位价格回填缺失 SKU 价格；
- daily 销量重复计权；
- 广告/促销信号在竞争、价格、运营三维重复计算；
- 严重风险（BLOCKED_LOGISTICS/MARGIN_RISK）被综合分平均掉；
- 触碰 TD-3/8/9/10/11/12/14/16/17/18、WB/CEL 费率、T2 config 结构、T3 拆分结构、现有 UI 换皮。
