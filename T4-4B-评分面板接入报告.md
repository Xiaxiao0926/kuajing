# T4-4B 评分面板功能接入报告

> 授权：T4-4A 验收通过（λ=0.5 冻结）→ T4-4B 功能面板接入。
> 边界遵守：未改 λ/六维权重/评级线/Supply Gap；leader-share 未入分；无 AI 推荐；无全站视觉换皮。
> 顺序：Gate 0 fail-close → 配置唯一源链路 → 数据适配层 → React 展示。

---

## 1. Gate 0：fail-close（先于一切 UI）

| 防护 | 行为 |
|---|---|
| `scoring_rules` 缺 `scale_weight/scale_sales_weight/scale_units_weight` | `scoreDemand()` 抛 `SCORING_CONFIG_FAIL`，禁止静默回退旧 demand 语义 |
| HIGH/MEDIUM 但 `deps.marketScalePool` 未加载/空数组 | 同上抛错 |
| LOW / LOW_MARKET_CONTEXT | 不需要 pool，不抛（语义本就回退候选强度，属冻结契约） |

单测新增测试 20（4 断言：HIGH/MEDIUM/空池/rules 缺字段 全部抛错），unit 73→**81/81**。
浏览器端不再维护第二份 0.5/0.6/0.4：面板直接 import 生成的只读规则。

## 2. scoring_rules.json 纳入 T2 配置唯一源链路

- `scripts/sync-config.js`：新增 `scoring_rules.json → src/generated/scoring_rules.js`，带结构校验（demand 三个 scale 参数必为正数、sales+units 子权重=1、六维权重齐备），生成物 **deepFreeze 只读**。
- `scripts/verify_sync.js`：新增"生成物与 config/scoring_rules.json 逐字符一致"检查（npm run test:sync 内）。
- 浏览器只消费 `src/generated/scoring_rules.js`；没有手写复制副本。

## 3. 数据适配层（无逻辑进 JSX）

新增 `ozon-react/src/utils/scoring/scoringDataAdapter.js`（纯函数）：

```text
raw canonical candidates（public/data/scoring_candidates.json，由 scripts/build-scoring-input.js 从
选品 xlsx 生成，解析实现唯一化到 scripts/scoring-xlsx.js）
  → buildBsrIndex（855 类型 + 19 域 + marketScalePool 全局池）
  → matchType（精确>包含）/ blendBenchmark（shrinkage）/ buildMarketContext
  → buildCandidatePool + fillLogisticsPools（尺寸无效不入池）
  → makeCelChannels（ozonEngine calcShipping）
  → scoreProduct() → buildExplanations() → ScoredProduct[]
```

`scripts/t4-score-audit.js` 已重构为**同源调用本适配层**（删掉本地重复的匹配/shrinkage/池代码 180 行），重跑审计分布逐位一致（A1/B191/C448/D169/191、LMC 54.7、mapped 61.0）。

## 4. 面板（NewDashboard → ProductScoringSection）

`ozon-react/src/components/dashboard/sections/ProductScoringSection.jsx`（编排层，只渲染）：

- **总览**：SKU 总数、A/B/C/D/不可评级计数、ELIGIBLE 数、HIGH/MEDIUM 数、平均分。
- **排名表**（1000 行，粘性表头）默认排序 = **Decision 优先级 → 综合分 → Evidence Coverage**（86+BLOCKED 不会排在 78+ELIGIBLE 前，已用不变量测试锁定）。列含：综合分/等级/Decision/市场规模/候选表现/竞争/利润/物流/Supply Gap/Context/风险标记。
- **筛选**：等级、Decision、Context、Supply Gap、风险（毛利/合规/物流/数据不足）、类目（234 类目下拉）。
- **单 SKU 详情**：基础信息、总分+Evidence、六维条形（demand 拆两行：├ 市场规模 / └ 候选表现）、Why 优势/风险（直接调 `buildExplanations()`，无第二套规则）、Market Context（BSR 类型/样本 n/置信/价格带 P25/P50/P75）、Supply Gap 明细、Decision 下一步。
- 加载态 / **fail-close 错误态**（初始化失败时显示错误，不显示旧算法分数）。

## 5. 性能（先实测，后做零行为变化优化）

| 阶段 | 耗时 |
|---|---|
| 首次完整评分（优化前，Node 同引擎） | 1953 ms |
| 定位 | `percentileRank` 每次对 1000 长池复制排序（~40 次/SKU × 1000 SKU） |
| 优化 | 池升序预排序一次 + `isSortedAsc` 检测跳过重复排序（结果数学上逐位一致） |
| **首次完整评分（优化后）** | **150 ms**（13×），含解释生成 |
| 筛选/排序 | 毫秒级（useMemo 缓存，不随 rerender 重跑 1000 SKU） |

优化为**行为零变化**：`isSortedAsc` 通过的池与"复制后排序"给出同一升序序列。验证：81+20+56 全绿，适配层回归 13/13，审计分布逐位一致（见 §6）。

## 6. 验收清单

| 项 | 结果 |
|---|---|
| 1000 SKU UI 实际加载 | `scoring_candidates.json`（666 KB/1000 行）随 vite 构建进 dist，preview HTTP 200 |
| 评分条数 / A-B-C-D-null 与审计一致 | ✅ 1/191/448/169/191（适配层回归测试锁定） |
| 筛选结果 | 六维筛选全实现；筛选计数实时显示 |
| 随机 5 SKU：UI vs audit 逐位一致 | ✅ index 3/77/213/555/888 全字段一致（totalScore/grade/六维/两分量/supplyGap/decision/status） |
| Decision/Gate 显示正确 | ✅ 排序不变量 + 状态/动作中文映射 + 风险徽标 |
| LOW_MARKET_CONTEXT 文案正确 | ✅ 三档口径（同类市场/对应 BSR 市场域/候选池表现）由 buildExplanations 输出，golden 56 断言锁定 |
| 首次评分耗时 | 150 ms（面板内显示实耗时） |
| 全量测试 | `npm test` = wbEngine 65 + scoring **81+20+13** + golden 56 + python 31 ✅；`test:golden` 76/76；`test:sync` 零差异+scoring_rules 一致性；`test:web-persistence` ✅ |
| build | ✅ vite build 通过（评分代码在懒加载 MarketResearch chunk，含 fail-close 字符串） |
| 浏览器 smoke | 生产构建 `vite preview` 冒烟：index 200、两个评分数据文件 200、懒加载 chunk 含评分面板代码。本环境无 headless 浏览器，未做真实渲染截图——建议你本地 `npm run dev` 打开市场调研页自查一次 |

## 7. 需要你知晓的数据决策

`ozon-react/public/data/scoring_candidates.json`（1000 行 canonical 候选，含真实价格/销量/佣金）随本次提交进入公开仓库 `public/data/`——与现有热销 xlsx、BSR 基准同目录同策略（静态部署必须）。若你认为这 1000 行候选数据不应公开，我可以把它移到 `.gitignore` 并改为构建期生成，请指示。

## 8. 文件清单

- 新增：`ozon-react/src/utils/scoring/scoringDataAdapter.js`、`scoringDataAdapter.test.mjs`、`ozon-react/src/components/dashboard/sections/ProductScoringSection.jsx`、`scripts/scoring-xlsx.js`、`scripts/build-scoring-input.js`、`ozon-react/src/generated/scoring_rules.js`、`ozon-react/public/data/scoring_candidates.json`
- 修改：`scoringEngine.js`（Gate 0）、`normalization.js`（isSortedAsc 零变化优化）、`scoringEngine.test.mjs`（81）、`NewDashboard.jsx`（接入）、`package.json`（test:scoring 链）、`scripts/sync-config.js`、`scripts/verify_sync.js`、`scripts/t4-score-audit.js`（同源重构）
