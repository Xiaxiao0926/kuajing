# T2 Gate 0 — CEL 产品资费表 V5.23 核验报告（TD-15）

> 日期：2026-08-14
> 目的：T2 建立 config 唯一事实源之前，核对 Ozon rFBS 渠道（尤其 HK）与代码 `ozonEngine.js` 的一致性。
> 性质：**只读核验，未改任何代码/配置。**
> 原始文件：`运费计算/CEL产品资费表 V5.23.xlsx`，工作表 `OZON-rFBS`。

---

## 1. HK 渠道五项核验结论（原表第 18 行）

| # | 问题 | 原表原文 | 结论 |
|---|---|---|---|
| 1 | `96` 的单位 | **9.6元/100克 + 19元/票** | `96` 是**元/kg**（9.6元/100克 = 96元/千克）。代码数值正确，但配置字段 `rateUnit: 'per100g'` 标签错误 |
| 2 | `19` 是否为每票固定费 | 19元/票 | ✅ 是每票固定费 |
| 3 | 是否按 100g 向上取整 | 计费逻辑原文：**"百克进位"** | ✅ 是；代码 `ceil(kg×10)/10` 与之一致 |
| 4 | 三边和>60cm 体积重 ÷6000 | **"尺寸三边和超60cm计算体积重（长x宽x高÷6000）…三边和不超60cm按实重计费"** | ✅ ÷6000，阈值 60cm 均正确 |
| 5 | 计费重量取 max(实重, 体积重) | **"并与实重比较取最大值计费"** | ✅ 正确 |

**HK 其他字段核对**：时效 7-12天 ✅；重量 0.001-25KG（计抛）✅；货值 1-500000₽ ✅；三边和≤310CM/单边≤150CM ✅——与代码全部一致。

**结论**：代码计算逻辑与数值**正确**；矛盾只在**单位表述层**：
- 配置字段 `rateUnit: 'per100g'` 是错的（96 应为元/kg）；
- UI 曾显示"9.6元/KG (96元/100g)"自相矛盾（9.6元/KG 应为 9.6元/100克）。

**T2 迁移时**：`config` 中 HK 应存储为 `kg_rate_cny: 96` + `weight_rounding_g: 100` + `volumetric: conditional ÷6000 @三边和>60`，字段语义与 WB 配置统一；**不沿用 `per100g` 标签**。

## 2. 附注：其他渠道抽查（与代码对比，全部一致）

| 渠道 | 原表费率（元/克 → 元/kg） | 代码 | 结论 |
|---|---|---|---|
| Extra Small | 0.0468/0.0364/0.026 + 3.12元/票 | 46.8/36.4/26 + 3.12 | ✅ |
| Budget | 0.03432/0.026/0.01768 + 23.92 | 34.32/26/17.68 + 23.92 | ✅ |
| Small | 0.0468/0.0364/0.026 + 16.64 | 46.8/36.4/26 + 16.64 | ✅ |
| Big | 0.026/0.01768 + 37.44，体积重÷12000，体积重≤31kg | 26/17.68 + 37.44，volDiv 12000，chargeWeightMax 31 | ✅ |
| Premium Small | 0.0468/0.0364/0.026 + 22.88，实重计费 | 46.8/36.4/26 + 22.88 | ✅ |
| Premium Big | 0.02912/0.02392 + 64.48，体积重÷12000，≤80kg | 29.12/23.92 + 64.48，chargeWeightMax 80 | ✅ |

## 3. 两个非阻塞观察（登记，不修改）

1. **Big 渠道重量下限**：原表 `2.001-30KG`，代码 `weightMin: 2`（差 0.001kg 边界）。属于边界表述差异，无实际影响，T2 迁移时以原表 2.001 为准。
2. **HK 退运服务**：原表"退运收取正向运价×1.5倍"——代码未实现退运计价（当前引擎只算正向）。如需支持需单独立项，不在 T2 范围。

## 4. 对 TD-15 的定性

TD-15（HK 费率单位存疑）**已解决**：代码数值正确，仅配置字段标签与 UI 文案错误。T2 迁移时按本报告 §1 的结论表述（96 元/kg + 百克进位），并在 config schema 中明确 `rate_unit` 字段语义，杜绝再次出现单位歧义。
