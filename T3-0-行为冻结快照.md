# T3-0 UI/Bundle 行为冻结快照

> 日期：2026-08-14
> 分支：`refactor/v3-t3-react-structure`（基于 main `76c5934` = tag `v3-t2-config-source`）
> 原则：T3 前后用户不应该察觉项目被重构过。任何行为/视觉/数据差异 = 失败。

---

## 1. Bundle 基线（Before）

| 指标 | 值 |
|---|---|
| 主 JS chunk | `index-*.js` 2,743.04 kB（gzip 779.66 kB） |
| 其他 JS chunks | es-locale 150.69 kB、purify 24.29 kB |
| CSS | 54.08 kB（gzip 9.05 kB） |
| build 时间 | ≈14.8s（含 config sync） |
| dist 总量 | 11.99 MB（含 public/data 数据拷贝，属 TD-9 范围，T3 不动） |

已知警告（**T3 不处理**）：NewDashboard.jsx 词库字典存在大量 duplicate key（后者覆盖前者，运行时行为不变）；chunk >500kB 警告。

## 2. UI 行为基线（T3 前后必须一致）

### 2.1 导航与入口
- `App.jsx` 按 roadmap 节点映射组件：n1 选品立项(ProjectSetup) / n2 市场调研(MarketResearch) / n36 香薰调研(FragrancePricing) / n4 Ozon核算(OzonCalc) / n39 WB核算(WBCalc) / n5 产品定义 / n6 供应链 / n8 成本报价 / n9 合规评估 / n14 Listing内容 / 其余占位 NodePage。
- Sidebar 7 个 phase 分组、节点状态持久化 key：`roadmap-statuses`（persistGet/persistSet）。
- 文件上传：Sidebar 拖拽/点击 + 服务端 `/data/manifest.json` 30s 轮询；数据格式自动判别（新格式含销售额/曝光量字段 → 直接 setData；旧格式走 cleanData→addPriceCategory→calculateKPIs）。

### 2.2 关键默认值（不得变）
- OzonCalc SingleTab 初始参数：price 5200₽、weight 1.55kg、52×45×28cm、purchaseCost 35、domesticShipping 3、labelingFee 2、commission 12、adRate 10、paymentFee 1、agencyFee 2、returnLoss 4；持久化 key `shipping-calc-rfbs-v2`。
- OzonCalc MultiTab 产品：发膜/精油喷雾/家用手套；持久化 key `product-pricing-v2`。
- WBCalc 持久化 key：`wb-settings-v1` / `wb-tariffs-v1` / `wb-skus-v1` / `wb-orders-v1`；默认 settings 来自 `DEFAULT_SETTINGS`（rubPerCny 12）。
- WB 佣金数据：fetch `/data/wb_commission.json`（96 类目/7424 条），组件内 `_commissionCache` 缓存。

### 2.3 计算语义（冻结自 T2，拆分不得改变）
- SingleTab 输入"售价"直接算（**不乘 0.6**）；MultiTab 输入"上架价"×0.6（TD-14 保持原样）。
- HK 渠道显示"96元/KG · 100g进位"；`rateUnit:'per100g'` 为引擎内部分支标志。
- WB 引擎全部纯函数，组件只调用 `wbEngine.js` 导出。

### 2.4 交互行为（不得变）
- 各 Tab 切换、筛选/排序/分页、CSV 导入导出、费率编辑保存（失败提示+session 恢复）、重置为默认、订单标签（正常签收/一倍赔偿/两倍赔偿/待账单确认/预计与实扣不一致）。
- localStorage 持久化 key 集合不得增删改。

## 3. 拆分规则（T3 执行铁律）

1. 页面组件只做编排；展示组件做 presentation；状态逻辑进 hooks；**计算逻辑留在现有 engine 不动**。
2. 迁移以"逐字搬移"为准：组件边界只加 import/export，不改 prop 名、不改默认值、不改事件处理、不改条件分支。
3. 不拆成几十个 20 行只用一次的小组件；不为拆而拆。
4. 每阶段独立 commit + 全套测试（npm test / test:golden / test:sync）+ build。
5. 完成后主 chunk 应有明显下降（TD-6），首屏不需要的模块进入独立 chunk。

## 4. 阶段计划

- T3-1 拆 NewDashboard.jsx（459KB）
- T3-2 拆 WBCalc.jsx（1692 行）
- T3-3 拆 FragrancePricing.jsx（42KB）
- T3-4 React.lazy + Suspense 页面级分割
- T3-5 回归验收 + 文档更新
