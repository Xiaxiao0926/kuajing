# T4-1A BSR 市场基准层设计与映射审计报告

> 日期：2026-08-14
> 分支：`feat/v3-t4-product-scoring`
> 性质：**只建数据基准层与映射审计，未写任何评分公式、未定任何权重。**
> 产出资产：`ozon-react/public/data/bsr_market_benchmarks.json`（974KB，纯聚合，无商品明细）
> 构建脚本：`scripts/build-bsr-benchmark.js`（可复跑）

---

## 1. 输入数据事实

| 指标 | 值 |
|---|---:|
| BSR 文件数 | 19（2026-07-22 ×3、2026-07-23 ×16） |
| 原始行数 | **19,000** |
| 产品链接唯一数 | **18,483**（跨文件重复 515，已按链接去重） |
| 产品类型数 | **855**（+1 个"(未分类)"） |
| 产品类型样本中位 | 5；≥5 样本 441 个；≥10 样本 278 个（去重后口径） |
| BSR 标签覆盖率 | 85.1%（"(空)"仅 14.9%；"未标记"为显式标签 15.0%） |

## 2. 字段格式审计（6 套格式 → canonical schema）

### 2.1 两大家族

- **格式 A（13 文件）**：表头带后缀，如 `销售额(₽)`、`销量(件)`、`下单转化率(%)`；
- **格式 B（6 文件）**：表头无后缀，如 `销售额₽`、`销量`、`平均销售价格₽`、`增长趋势`、`平均每日销量`。

已建立完整别名映射（`build-bsr-benchmark.js` 内 `ALIAS` 表，55 条）。

### 2.2 转化率双编码（已验证）

| 编码 | 证据 |
|---|---|
| 格式 A：`0.0032` = **0.32%** | 格式 A 文件 conv p50 全落在 0.002-0.0045；与销量/曝光交叉量级吻合 |
| 格式 B：`0.38` = **0.38%** | 格式 B 文件 conv p50 全落在 0.14-0.30；两族归一后 p50 同量级（0.2%-0.4%） |

**归一规则（已实现）**：格式 A 百分比字段 ×100；格式 B 不变；winsorize(1%-99%)；明显越界（>500%）判脏剔除。

### 2.3 销量口径确认（Q1 已解）

`销量 ÷ 日均销量` 中位 = **28.54**（15,324 有效样本）；`销售额 ÷ 日均销售额` 中位 = 28.52。
→ **正式规则：`units/sales_rub` = 近 28 天累计，作为需求主指标；`daily_*` 仅一致性校验与展示，不重复加权。**

## 3. 两级市场层次与聚合基准（已生成）

```
bsr_market_benchmarks.json
├─ meta（单位说明/去重统计/样本分级/别名表）
├─ domains（19 个域基准：汽车/宠物/家具/家装/成人/手机穿戴等）
└─ product_types（855 个产品类型基准）
```

每个基准节点包含（P10/P25/P50/P75/P90 视字段而定）：
`sales_28d`、`units_28d`、`avg_price_rub`、`min_price`、`sign_rate`、`conv_rate`、`cart_add`、
`discount`、`promo_share/promo_days`、`ad_days/ad_roi`、`oos_days`、`missed_sales_rub`、`volume_l`、
**结构指标**：`brand_hhi`、`seller_hhi`、`top1/top5 品牌份额`、`top10 卖家份额`、`fbo/fbs 份额`、`bsr_leader_share`、
**supply_gap 分量**（sales_p50 + oos_days_p50 + missed_sales_p50 + seller_hhi，仅原始分量，未合成指数）。

样本量分级（回退规则输入）：
- `n ≥ 10`：278 类型 → 可用类型自身基准；
- `5 ≤ n < 10`：161 类型 → 叶子 + 域基准 shrinkage；
- `n < 5`：417 类型 → 主要用域基准；
- 无匹配 → 全局基准 + `LOW_MARKET_CONTEXT` 标记。

## 4. 候选 → BSR 映射覆盖率（关键结论）

| 档 | 类目数 | 行数 | 占比 |
|---|---:|---:|---:|
| 精确匹配 | 56 | 208 | 20.8% |
| 包含匹配 | 10 | 21 | 2.1% |
| **未匹配** | **168** | **771** | **77.1%** |

**未匹配大头**（按行数）：维生素 52、餐巾纸 33、垃圾袋 31、土壤 31、指甲护理 24、管道连接器 24、割草机线 23、调味料 23、保护玻璃 21、即食 20……

**原因**：BSR 19 份表覆盖 19 个大域（汽车 15 个 + 宠物/家具/家装/成人/手机穿戴），而候选 1000 行横跨 234 个叶子类目，含大量 **食品/美妆/日化/家居**（BSR 未采样的赛道）。

### 由此确定的评分数据策略（供 T4 评分模型使用，非本报告已实现）

```
候选 SKU
  ├─ 映射命中（≈23% 行） → 产品类型基准（按样本量分级回退到域/全局）
  └─ 映射未命中（≈77% 行） → 全局基准 + status: LOW_MARKET_CONTEXT
                             （评分解释中显式标注"市场上下文不足"，
                              相关维度降权，绝不编造市场基准）
```

## 5. BSR 标签的处理定论

标签分布：Top-50% 42.4%、销售领导者 19.8%、未标记 15.0%、(空) 14.9%、Top-100% 5.3%、Super 2.5%。

**定论**：
1. BSR 标签**不作为评分输入**（标签与销售额量级无稳定线性关系：销售领导者中位 54.4 万₽ > Top-100% 34.1 万₽ > Super 23.9 万₽，Super 并非更高一级）；
2. 标签用途 = **模型验证**：评分模型产出高分集后，检查其中"销售领导者"占比是否显著高于全体 19.8% 基线；
3. 标签缺失 ≠ 商品差（14.9% 空值包含正常商品）。

## 6. 已交付/未交付边界

| 项 | 状态 |
|---|---|
| 19 文件 canonical 化 + 别名 + 百分比归一 | ✅ 已实现并验证 |
| 按链接去重（515 剔除） | ✅ |
| domain/类型两级基准 + 结构指标 + supply_gap 分量 | ✅ |
| `bsr_market_benchmarks.json` 纯聚合资产 | ✅ 974KB（不含 19,000 明细） |
| 映射覆盖率审计 | ✅ 本报告 §4 |
| 评分引擎 / 权重 / UI | ❌ 未做（T4-2 起，等你定模型） |

## 7. 等你决策（T4 评分模型输入）

1. 六维权重终值（建议参考：需求 25 / 竞争 15 / 价格 10 / 利润 20 / 物流 15 / 运营 15 = 100，本报告仅列数据事实不表决策）；
2. 未匹配 77% 行的处理：全局基准 + 降权 + LOW_MARKET_CONTEXT，还是先只对 23% 可映射行出 A/B 评级、其余仅出候选自身分？
3. `supply_gap` 合成公式（各分量权重）与触发阈值；
4. A/B/C/D 评级阈值。
