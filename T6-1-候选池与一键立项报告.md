# T6-1 候选池与一键立项报告

> 分支：`feat/v3-t6-sku-project-lifecycle`（自 main `1846c68`）。T6-0 V1.1 硬化规格已冻结，本阶段按边界实现。
> 冻结：T4 模型零改动；评分分布 1/191/448/169/191 不变；未做 Supplier/CostScenario 联动/Gate 引擎/合规域/订单运营（留 T6-2+/T7/T8）。

---

## 1. 数据层（t6Store，逐实体存储）

- 存储 key：`t6.candidate.<uuid>` / `t6.project.<uuid>` / `t6.snapshot.<uuid>` / `t6.log.<uuid>`（UUID 主键；`RU-YYYY-NNN` 仅人类编号 + `marketCode: 'RU'`）；
- ScoringSnapshot = `structuredClone(scoreProduct 输出)` + explanations + sourceInputs（本次实际 canonical 输入）+ marketContext（matchMethod/domain/benchmarkGeneratedAt）+ versions（rules/engine/dataset/benchmark）；
- 不可变性在 API 层面成立：**不存在 updateSnapshot/deleteSnapshot/updateLog/deleteLog**；写入深拷贝，外部修改不影响已存记录；
- 人工 bizStatus 与模型 Grade/Decision 正交；`projectIds.length>0 → 已产生项目` 为派生事实；
- lifecycleStatus（DRAFT/ACTIVE/PAUSED/ARCHIVED/KILLED）× stage（PIPELINE..REVIEW）；PAUSED 恢复保留原 stage；`roadmap-v1` 模板冻结（nodeId/phaseId/title/order），新项目全 pending、不继承旧 `roadmap-statuses`；
- DecisionLog 统一记录 candidate/project 两域（加入候选、刷新评分、状态流转、一键立项、生命周期动作）。

## 2. 功能（T6-1 边界内）

- **选品评分页**：表格行 [加入候选]；Drawer [加入候选] [创建项目]（一键立项 = 确保候选 → 生成/复用不可变快照 → 创建项目 → 自动 ACTIVE）；
- **候选池页**（导航：商品中心 → 候选池）：列表含 商品/评分/Decision/Context/市场规模/候选表现/利润/物流/Supply Gap/负责人(可编辑)/状态(人工下拉)/操作；[刷新评分] 生成新快照并更新 `latestSnapshotId`（旧快照不变）；[创建项目] 复用当前快照；
- **SKU 项目页**：项目卡片（编号/生命周期徽标/阶段/进度 N/36/立项快照 Score+Decision+Context/下一步节点/立项快照日期与规则版本）；生命周期动作（启动/暂停/恢复/归档/淘汰）写入决策日志（原因 prompt）；
- 每项目 workflow 以 roadmap-v1 模板初始化为 36 节点全 pending。

## 3. 稳定身份（审核项 1 落地）

- `scoring-xlsx.js` 解析 `商品ID` → `source_product_id`（**1000 行 / 1000 非空 / 1000 唯一**，已验证）；
- `scoring_candidates.json` 重建（698KB）；评分引擎忽略该字段，**冻结分布不变**（适配层回归锁）；
- Candidate 身份 = sourceProductId；candidateIndex 仅记录不参与身份。

## 4. T6-0 冻结的 8 条 invariant（t6Store.test.mjs，20/20 全绿）

| # | invariant | 结果 |
|---|---|---|
| 1 | 源数据重排 → 候选仍指同一商品 | ✅ |
| 2 | 刷新评分 → 旧快照字节不变 | ✅ |
| 3 | 项目创建后刷新候选 → creationSnapshotId 及快照不变 | ✅ |
| 4 | 一个候选 → 多个项目（RU-2026-001/002 唯一编号） | ✅ |
| 5 | PAUSED → 恢复仍回原 stage | ✅ |
| 6 | 旧快照/日志无 update/delete API | ✅ |
| 7 | roadmap-statuses 字节不变、无跨命名空间写 | ✅ |
| 8 | 评分分布前置（1000 唯一 source_product_id）+ 分布回归 | ✅（1/191/448/169/191） |

## 5. 回归

`npm test`（wbEngine 65 + scoring 81/20/13/22 + **t6 20** + golden 56 + python 31）、`test:golden` 76、`test:sync` 零差异、`test:web-persistence`、build、`verify:scoring-build` 全绿。

## 6. 未做（按边界，留给后续）

Supplier UI、CostScenario 与 Ozon/WB 正式联动（自动带入映射已定稿未接线）、Stage Gate 引擎（含 NOT_EVALUATED）、合规数据域、订单/运营数据；项目详情六 Tab 页（T6-2）。
