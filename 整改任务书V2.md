# D:\ozon 项目整改任务书 V2（可执行版）

> 版本：V2（取代 V1 方案）
> 状态：待执行
> 适用对象：Codex 执行，人审阅
> 前置原则：**未扫描不 push；先快照后清理；不迁移技术栈；不修改已通过测试的业务计算逻辑。**

---

## 0. V1 → V2 变更记录（评审采纳项）

| # | 变更 | 来源 |
|---|---|---|
| 1 | P0 改为「Secret Audit → 修复远程 → 完善 gitignore → 本地快照 → Secret Scan → 正式 commit → push」 | 评审① |
| 2 | P2-1 改为「独立 JSON 配置层为唯一事实源，React/Python 均为消费者」，不选任一端 | 评审② |
| 3 | P2-2 删除「每文件 <300 行」硬指标，改为「按职责拆分 + 全部回归测试通过」 | 评审③ |
| 4 | `_archive/` 细分 `debug/ analysis/ migration/ reports/ deprecated/`，按知识价值分类，不机械全归档 | 评审 |
| 5 | E 盘路径三层优先级（环境变量 → 项目 data/ → Legacy），并加 `LEGACY_PATH_ENABLED` 开关，不一次性砍断 | 评审 |
| 6 | 「首屏 <3s」注明测试条件（设备/网络/冷启动/构建模式） | 评审 |
| 7 | 新增 P3 业务增强阶段（1201 行选品数据 → SKU 评分） | 评审 |

---

## 1. 执行总则（铁律）

1. **未做 Secret Audit 与 Scan，禁止任何 push 到 GitHub。**
2. **先备份后清理**：任何删除/移动操作前必须已有本地安全快照。
3. **不迁移技术栈**：React/Vite、Streamlit、Node/Express 保持不变。
4. **不碰已锁定的计算规则**：`wbEngine.js` 的 65 项测试与 `wb_test.py` 是护栏，任何改动后必须全绿；改动仅限「配置来源」层。
5. **每步可回滚**：P0/P1 通过 git 快照回滚；P2 通过配置层开关（保留旧代码路径）回滚。
6. **禁止事项**（全阶段）：不得把 `.env`、`ai_config.json`、`*.pem`、`*.key`、cookies、任何含密钥文件提交或推送；不得删除 `市场分析/`、`选品/`、`运费计算/` 内任何业务数据文件；不得把 `node_modules/`、`dist/`、`_archive/` 推入远程（`_archive/` 是否入库待定，见未决项 U2）。

---

## 2. 前置输入清单（执行前需用户确认/提供）

| 编号 | 需要用户提供 | 用途 |
|---|---|---|
| U1 | GitHub PAT（或已登录的 credential manager / SSH key） | P0-1 修复远程凭证 |
| U2 | `_archive/` 是否提交进 GitHub？（默认：**不提交**，仅本地保留） | P1-1 |
| U3 | 仓库 kuajing 是 public 还是 private？（若 public：业务数据 xlsx 是否可公开，需单独决策） | P0-3 提交范围 |
| U4 | `E:\Desktop\坪优报价分析` 的文件监听流水线当前是否仍在使用？ | P1-4 是否保留 Legacy 路径 |
| U5 | `ai_config.json` 当前是否真实存在于 `ozon-product-analyzer/`？（默认策略：确认被 gitignore 且不入库） | P0-0 |

---

## 3. P0 — 安全与备份（最高优先级）

### P0-0 Secret Audit（本地扫描，不联网）

**目标**：找出 40+ 未提交文件中所有疑似敏感信息，产出审计报告，决定哪些文件绝不入库。

**命令**（PowerShell，在 D:\ozon 执行）：

```powershell
# 1) 候选文本文件清单（排除 node_modules/.git/dist，含隐藏文件）
$inc = @('*.js','*.mjs','*.cjs','*.jsx','*.ts','*.tsx','*.py','*.json','*.md','*.txt','*.html','*.htm','*.css','*.yaml','*.yml','*.example','*.conf','.env','.env.local','.npmrc','.gitconfig')
$files = Get-ChildItem D:\ozon -Recurse -Force -File |
  Where-Object { $inc -contains $_.Extension -and $_.FullName -notmatch 'node_modules|\\.git\\|\\dist\\|_archive' }

# 2) 高危模式扫描（-List 只报首个命中，避免刷屏）
$patterns = 'API_KEY','api_key','apikey','api-key','TOKEN','token=','SECRET','secret=','PASSWORD','passwd','Authorization','Bearer','github_pat_','ghp_','gho_','sk-[A-Za-z0-9]{20,}','AKIA[0-9A-Z]{16}','BEGIN RSA PRIVATE KEY','BEGIN PRIVATE KEY','-----BEGIN','cookies.json','credentials','session=','x-api-key'
$hits = Select-String -Path $files.FullName -Pattern ($patterns -join '|') -List -ErrorAction SilentlyContinue
$hits | Select-Object Path, LineNumber, @{n='Match';e={$_.Matches[0].Value}} | Format-Table -AutoSize
$hits | Export-Csv -Path D:\ozon\_audit\secret_audit_p0.csv -NoTypeInformation -Encoding UTF8
```

```powershell
# 3) 私钥/证书/凭据文件直查
Get-ChildItem D:\ozon -Recurse -Force -File |
  Where-Object { $_.Name -match '\.(pem|key|p12|pfx|cer|crt)$' -or $_.Name -match '(?i)cookie|credential|secret|token|password' -or $_.Name -match '^\.env' } |
  Where-Object { $_.FullName -notmatch 'node_modules|\\.git\\|\\dist\\' } |
  Select-Object FullName, Length | Format-Table -AutoSize
```

```powershell
# 4) 扫描已入库的 init commit（历史污染检查）
git -C D:\ozon grep -I -n -E 'API_KEY|SECRET|TOKEN|PASSWORD|ghp_|sk-[A-Za-z0-9]{20,}|BEGIN (RSA )?PRIVATE KEY' 747f254 -- . ':(exclude)*.xlsx' 2>$null
```

```powershell
# 5) 大文件清单（GitHub 单文件 100MB 红线；>5MB 列出人工确认）
Get-ChildItem D:\ozon -Recurse -File |
  Where-Object { $_.Length -gt 5MB -and $_.FullName -notmatch 'node_modules|\\.git\\' } |
  Sort-Object Length -Descending |
  Select-Object @{n='MB';e={[math]::Round($_.Length/1MB,1)}}, FullName | Format-Table -AutoSize
```

**人工判定规则**：
- 命中真实密钥（如 `sk-...`、`ghp_...`、AWS AKIA）→ 列入「绝密清单」，确认文件已/将 gitignore，且**密钥需作废轮换**（如曾推送过则必须换）；
- 命中仅为变量名/占位符（如 `TOKEN = ''`、`process.env.XXX`）→ 放行，记录到审计报告；
- 业务数据 xlsx 含供应商价格/联系方式 → 取决于 U3（仓库 public/private）。

**验收**：`D:\ozon\_audit\secret_audit_p0.csv` 生成；「绝密清单」为空或已获用户确认处理方式。

**回滚**：本步只读，无需回滚。

---

### P0-1 修复远程凭证

**目标**：`git fetch` 成功。

**命令**：

```powershell
git -C D:\ozon remote -v            # 确认 remote 地址
git -C D:\ozon fetch --dry-run      # 复现错误
# 方案 A（推荐）：GitHub PAT
git -C D:\ozon config credential.helper manager
# 然后任意一次 fetch 弹窗输入 PAT（或由用户预先在 Windows 凭据管理器配置）
# 方案 B：改 SSH
# git -C D:\ozon remote set-url origin git@github.com:Xiaxiao0926/kuajing.git
```

**验收**：`git fetch --dry-run` 无 schannel 错误。

**回滚**：`git config --unset credential.helper` 或还原 remote URL。

**依赖**：U1（用户提供凭据）。

---

### P0-2 完善 .gitignore

**目标**：敏感文件、临时产物、生成物全部排除。

**待追加/确认项**（现有基础上）：

```gitignore
# 审计与整改产物（本地保留）
_audit/
_archive/
整改方案.md
整改任务书V2.md

# 配置密钥（已有，确认覆盖全路径）
**/ai_config.json
*.pem
*.key
.env
.env.*

# 生成物
ozon-react/src/generated/
config/generated/
```

**验收**：`git status --short` 中不再出现 `_audit/`、`_archive/`、任何 `.pem/.key/.env`。

**回滚**：`.gitignore` 是普通文件，git 可还原。

---

### P0-3 本地安全快照（不 push）

**目标**：把全部有价值内容纳入一个本地 commit，作为后续所有操作的回滚点。

**命令**：

```powershell
git -C D:\ozon add -A
git -C D:\ozon status --short | Measure-Object          # 记录将提交文件数
# 人工核对 status 清单：不得出现敏感文件、node_modules、dist
git -C D:\ozon commit -m "snapshot: 整改前全量安全快照 (P0-3)"
git -C D:\ozon tag snapshot-p0-2026-08-14
```

**禁止**：本步不执行 `push`；不执行任何删除。

**验收**：`git log --oneline -2` 显示新 commit 与 tag；`git status` 干净（除 .gitignore 声明的未跟踪项）。

**回滚**：`git reset --soft HEAD~1`。

---

### P0-4 Secret Scan（对即将推送的内容复审）

**目标**：对「将推送到远程的增量」做最终扫描——即 init commit 与新快照之间的 diff。

**命令**：

```powershell
# 复审新增内容（新快照相对 init 的增量）
git -C D:\ozon diff 747f254 HEAD --name-only | Out-File D:\ozon\_audit\push_increment_files.txt -Encoding UTF8
# 对增量文本文件执行与 P0-0 相同模式扫描（范围收窄到增量）
$incFiles = Get-Content D:\ozon\_audit\push_increment_files.txt
$incText = $incFiles | Where-Object { $_ -match '\.(js|mjs|cjs|jsx|ts|tsx|py|json|md|txt|html|htm|css|yaml|yml)$' }
foreach ($f in $incText) {
  $p = Join-Path 'D:\ozon' $f
  if (Test-Path $p) { Select-String -Path $p -Pattern 'API_KEY|SECRET|TOKEN|PASSWORD|ghp_|sk-[A-Za-z0-9]{20,}|AKIA[0-9A-Z]{16}|BEGIN (RSA )?PRIVATE KEY' -List }
}
```

**验收**：增量扫描零高危命中，或命中项已由用户书面确认可公开。

**回滚**：本步只读。

---

### P0-5 正式 commit + P0-6 push

```powershell
git -C D:\ozon push -u origin main
```

**验收**：GitHub 远程可见新 commit；`git status` 本地与远程一致。

**回滚**（若误推敏感信息）：`git revert`/`git reset` + **立即轮换密钥**（删除历史需 filter-repo，需另行评估）。

---

### P0-7 恢复测试基线

**目标**：确认整改前计算引擎全绿，作为后续所有阶段的对照基线。

```powershell
cd D:\ozon\ozon-react; node src/utils/wbEngine.test.mjs        # 期望 65 通过 / 0 失败
cd D:\ozon\ozon-product-analyzer; python wb_test.py            # 期望全部 PASS（若缺依赖则记录到 _audit/ 并请示）
```

**验收**：测试输出存档到 `D:\ozon\_audit\baseline_tests.txt`。

---

## 4. P1 — 工程卫生（P0 完成后）

### P1-1 归档一次性脚本（分类，不机械全搬）

**目标**：根目录脚本数 20+ → <5。

**归档目录结构**：

```text
_archive/
├─ debug/        # debug_*.js、check_*.js、test_direct.js 等纯调试
├─ analysis/     # analyze_prices*.js、analyze_csv_fields.js、analyze_sample.js、price_analysis.* 等分析脚本
├─ migration/    # jd-union-service 残留引用、旧数据迁移类
├─ reports/      # _tmp_xunhang_full_report.txt、analysis_output.txt 等有业务价值的输出
└─ deprecated/   # 明确废弃且无参考价值的（用户确认后删除也可）
```

**分类规则**（逐文件判定，不允许按前缀批量搬）：
- 有业务分析价值/结论的 `.txt/.md/.csv` → `reports/`；
- 验证过业务规则的分析脚本 → `analysis/`；
- 纯调试、可随时重写的 → `debug/`；
- 确认完全无价值 → `deprecated/`（最终是否删除由用户决定）。

**命令**：

```powershell
New-Item -ItemType Directory -Force D:\ozon\_archive\debug, D:\ozon\_archive\analysis, D:\ozon\_archive\migration, D:\ozon\_archive\reports, D:\ozon\_archive\deprecated | Out-Null
git -C D:\ozon mv <文件> _archive/<子目录>/   # 逐个执行，用 git mv 保留历史
```

**禁止**：不得移动 `server.js`、`config.js`、`market_data_processor.js`、`analyze_with_cleaned_data.js`、`tunnel.js`（仍在用）、`package.json`；不得移动任何 xlsx/pdf/htm 业务数据。

**验收**：根目录脚本仅剩 `server.js`、`config.js`、`market_data_processor.js`、`analyze_with_cleaned_data.js`（+ 用户确认在用的）；`npm start` 仍可启动。

**回滚**：`git mv` 反向移动 + `git checkout -- .`。

### P1-2 删除 cc-switch 残留

```powershell
Remove-Item -Recurse -Force D:\ozon\cc-switch
```

**前置检查**：`cc-switch/.git` 无未推送的独立历史（与主仓库同源，已验证）。

**回滚**：不可逆删除 → 先 `git -C D:\ozon add -A && commit` 快照后再删；若意外，可从快照恢复。

### P1-3 根 README.md

**内容要求**：4 应用清单（ozon-react / server.js / ozon-product-analyzer / ozon_hair_dryer_analysis）、每个的启动命令、端口、数据目录、数据流图（市场分析 → vite 同步 → public/data；E 盘 → server.js 监听 → 分析结果）、测试命令、`_archive/` 说明、整改记录链接。

**验收**：按 README 从零可启动 4 个应用（在本机验证 2 个主用：ozon-react dev、server.js）。

### P1-4 路径配置化（三层优先级 + 开关）

**目标**：`config.js` 与 `ozon-product-analyzer/app.py` 不再硬编码绝对路径。

```javascript
// config.js（Node）
const LEGACY_PATH_ENABLED = process.env.LEGACY_PATH_ENABLED !== 'false'   // 默认 true
const BASE_PATH =
  process.env.BASE_PATH
  || path.join(__dirname, 'data')
  || (LEGACY_PATH_ENABLED ? 'E:\\Desktop\\坪优报价分析' : '')
```

```python
# ozon-product-analyzer/app.py
import os
LEGACY_PATH_ENABLED = os.environ.get('LEGACY_PATH_ENABLED', 'true').lower() != 'false'
DATA_DIR = (
    os.environ.get('OZON_DATA_DIR')
    or os.path.join(os.path.dirname(os.path.abspath(__file__)), '..', '选品')
    or ('d:\\ozon\\选品' if LEGACY_PATH_ENABLED else '')
)
```

**验收**：不设任何环境变量时行为与现在完全一致（Legacy 生效）；设 `BASE_PATH=...` 后改用新路径；`LEGACY_PATH_ENABLED=false` 且未设环境变量时**报错提示**而非静默走 Legacy。

**回滚**：开关默认 true，行为不变。

**依赖**：U4（E 盘流水线是否在用）。

### P1-5 测试脚本接入

根 `package.json`：

```json
"scripts": {
  "test": "node ozon-react/src/utils/wbEngine.test.mjs",
  "test:wb-py": "cd ozon-product-analyzer && python wb_test.py"
}
```

**验收**：`npm test` 输出 65/65；命令同时写入 README。

### P1-6 统一启动脚本（可选）

根目录加 `start-ozon-react.bat` / `start-server.bat`（双击启动），减少 README 操作成本。若用户不需要则跳过。

---

## 5. P2 — 架构收敛（P1 完成后）

### P2-1 独立 JSON 配置层（唯一事实源）

**目标**：费率/佣金/默认设置只有一份 JSON，React 与 Python 均为消费者。

**目录结构**：

```text
D:\ozon\config\
├─ wb_tariffs.json          # WB 费率（snake_case，与 Python 现有 tariffs.json 字段一致）
├─ ozon_channels.json       # Ozon CEL 渠道表（现固化在 ozonEngine.js 的 CHANNEL_GROUPS）
├─ commissions.json         # WB/Ozon 佣金表
├─ settings.json            # 全局设置（汇率、阈值等）
└─ schema/
   ├─ tariff.schema.json    # JSON Schema 校验
   └─ channel.schema.json
```

**数据迁移**：以 `ozon-product-analyzer/wb_data/tariffs.json`（snake_case）为初始内容基准，把 React `wbConfig.js` 中 camelCase 数据并入后删除其内嵌数据；`wbConfig.js` 改为**适配器**（snake_case → 现有 camelCase 导出），保证 `wbEngine.js` 与全部组件**零改动**。

**React 侧接入**：扩展 `vite.config.js` 现有 `ozonDataSyncPlugin` 模式，`buildStart` 时把 `D:\ozon\config\*.json` 拷贝到 `ozon-react/src/generated/`（gitignore），组件经适配器 import。**不用运行时 fetch**，避免异步化改造。

**Python 侧接入**：`wb_data.py` 的 `TARIFFS_FILE`/`SETTINGS_FILE` 改读 `D:\ozon\config\`（路径用环境变量 + 相对路径，同 P1-4 规则）。

**Schema 校验**：
- Python：启动时用 `jsonschema` 校验（若依赖未装，用内置 dict 结构校验兜底）；
- React：构建时在 vite 插件内用简单结构校验（不引入重依赖），或导出时校验。

**验收**：
1. 修改 `config/wb_tariffs.json` 中一个数值（如 DPX 0.4-20kg 档 `kg_rate_cny` 43→44）后：
   - `npm test` 中相关断言按预期变化（证明 React 侧已读 JSON）——**注意**：测试期望值基于 43，改动后应恢复原值，验证仅做临时性改-验-还原；
   - `python wb_test.py` 同样行为变化；
2. 双端对拍脚本（新增 `config/verify_sync.py` 或 `.js`）能证明 React 适配器输出与 JSON 内容一致；
3. 还原费率后全部测试通过。

**回滚**：`wbConfig.js` 保留内嵌数据的旧分支（`import.meta.env.VITE_USE_EXTERNAL_CONFIG === '1'` 时读外部，否则读内嵌），配置层验证通过后再删旧分支。

### P2-2 UI 按职责拆分（无行数硬指标）

**目标**：`NewDashboard.jsx`（459KB）、`WBCalc.jsx`（90KB）、`FragrancePricing.jsx`（42KB）职责分离。

**拆分标准**：
- 页面组件只做编排（orchestration）；
- 计算/引擎逻辑 → `utils/`（如 WBCalc 内计算已大部分在 `wbEngine.js`，继续外提剩余部分）；
- 表格/筛选器/弹窗/图表 → 独立子组件，单一职责；
- 每个拆出的组件以「被复用或可独立测试」为存在理由，**不为拆而拆**；
- 不设行数上限：一个职责清晰的 450 行组件是可接受的。

**验收**：
- 拆分前后功能一致（人工点击路径 + `npm test` 全绿）；
- 每个新组件有明确职责注释；
- 若某组件拆分无复用价值 → 保留原样并记录理由。

**回滚**：git 快照。

### P2-3 Vite 代码分割

**目标**：降低首屏体积。

**做法**：`React.lazy` + `Suspense` 按路由节点懒加载重型组件（`WBCalc`、`OzonCalc`、`NewDashboard`、`FragrancePricing`、`ListingContent` 等）。

**验收标准（注明条件）**：
- 测试设备：本机（Windows，Chrome 最新版）；
- 网络：localhost（dev）与 production build 各测一次；
- 口径：**production build + 空缓存冷启动**，DOMContentLoaded < 3s；
- dist 主 chunk 体积较当前（2.7MB）显著下降（记录前后数值到 `_audit/`）。

### P2-4 双端对拍（回归保障）

新增 `config/verify_sync.py`：读 `config/*.json`，分别调用 React 引擎（经 Node 子进程跑 `wbEngine.test.mjs`）与 Python `wb_calc`，对同一组边界用例（1g/100g/101g/300g/301g/20kg/超重）输出结果并 diff。

**验收**：对拍脚本全绿；纳入 `npm test` 或 README 例行命令。

---

## 6. P3 — 业务增强（P2 稳定后）

| 步骤 | 内容 | 验收 |
|---|---|---|
| P3-1 | 把 `选品/跨境项目产品线扩展计划.xlsx`（1201 行）接入 `ozon-product-analyzer` 评分面板 | 面板能输出 A/B/C/D 分级推荐 |
| P3-2 | 评分结果接入 React 前端（或导出 Excel） | 产出第一版「可测 SKU 清单」 |
| P3-3 | 评估接入 Ozon/WB API 的可行性（V1 不强制） | 可行性报告，不写代码 |

---

## 7. 全局验收清单

- [ ] GitHub 远程含全部源码，本地 `git status` 干净
- [ ] Secret Audit 报告在 `_audit/`，增量扫描零高危
- [ ] 根目录干净：4 个应用 + README + 业务数据目录 + `_archive/`
- [ ] `npm test` = 65/65；`python wb_test.py` 全 PASS；基线存档
- [ ] `config/*.json` 为唯一费率事实源，双端对拍一致
- [ ] 环境变量可切换数据路径，Legacy 行为默认不变
- [ ] 生产构建首屏达标（附测试条件记录）

---

## 8. 未决项（阻塞清单）

| 编号 | 问题 | 影响步骤 |
|---|---|---|
| U1 | GitHub 凭据未提供 | P0-1/0-6 阻塞 |
| U2 | `_archive/` 是否入库 | P1-1 |
| U3 | 仓库 public/private | P0-3 提交范围 |
| U4 | E 盘流水线使用状态 | P1-4 |
| U5 | `ai_config.json` 存在性确认 | P0-0 |
