# D:\ozon 项目整改方案

> 用途：提交 ChatGPT/评审人审阅。评审人可重点核对我标注的 **【决策点】** 与 **【验收标准】** 是否合理。

---

## 0. 背景结论（评审依据）

- 这是一个 **Ozon/WB 俄罗斯电商跨境运营工作区**，不是单一应用，而是 4 个应用 + 数据资产的混合体。
- **最宝贵的资产**：业务数据（热销品/供应链/费率 PDF）+ 已验证的计算引擎（WB 运费/利润 65 项测试全过）。
- **最大风险**：git 仅 1 个 init commit、40+ 文件未提交、远程凭证失效——**代码有丢失风险**。
- **最大技术债**：WB/Ozon 核算在 React 和 Python 双端各实现一套，规则易漂移。

---

## 1. 目标

| 序号 | 目标 | 衡量标准 |
|---|---|---|
| G1 | 代码安全 | 所有源码进入 git 且能 push 到远程，本地删除不丢 |
| G2 | 结构清晰 | 一次性调试脚本归入归档目录，根目录只剩可识别的应用/数据 |
| G3 | 规则单一化 | 费率/佣金只维护一份，双端同源 |
| G4 | 可上手 | 有根 README，新人在 10 分钟内能跑起 4 个应用 |
| G5 | 可回归 | `npm test` / `python wb_test.py` 一键跑通，改费率不怕回归 |

---

## 2. 执行方案（分 P0 / P1 / P2 三阶段）

### P0 — 数据安全与备份（第一天，1-2 小时）

| 步骤 | 具体动作 | 产出 | 验收标准 |
|---|---|---|---|
| P0-1 | 修复 git 远程凭证（`git credential` 重置或换 SSH） | 远程可访问 | `git fetch` 成功 |
| P0-2 | 处理 `.gitignore` 中已声明但仓库仍追踪/未追踪的二进制（`cloudflared.exe`、`_tmp_*`） | 干净 worktree | `git status` 无意外大文件 |
| P0-3 | **先做一次全量提交**（含所有有价值源码，暂不删东西） | 一个安全快照 commit | `git log` 出现新 commit |
| P0-4 | `git push -u origin main` | 远程备份 | GitHub 上可见最新代码 |

> **【决策点 1】**：P0 阶段「先提交后清理」还是「先清理后提交」？我建议**先提交（备份）后清理**，理由：清理过程可能误删，先有快照可回滚。请评审确认。

### P1 — 工程卫生与文档（第一周）

| 步骤 | 具体动作 | 产出 | 验收标准 |
|---|---|---|---|
| P1-1 | 建 `_archive/` 目录，移动 `_tmp_*`、`debug_*.js`、`analyze_prices_v2/v3/v4.js`、`check_*.js` 等一次性脚本 | 根目录脚本数 20+ → <5 | 根目录 `ls` 只剩 `server.js`、`config.js`、`market_data_processor.js`、`analyze_with_cleaned_data.js`、`package.json` |
| P1-2 | 删除 `cc-switch/`（仅含残留 `.git` 的嵌套仓库） | 消除嵌套仓库 | `cc-switch` 不存在 |
| P1-3 | 写根 `README.md`：4 应用清单 + 启动命令 + 数据流图 | 文档 | 按 README 可启动 ozon-react、server.js、两个 streamlit |
| P1-4 | `config.js` 的 `BASE_PATH` 改为「环境变量优先 + 项目内 `data/` 兜底」；`ozon-product-analyzer/app.py` 的 `DATA_DIR` 同理 | 去 E 盘依赖 | 换机器/换路径只需改 1 个环境变量 |
| P1-5 | 把 `wbEngine.test.mjs` 接入根 `package.json` 的 `test` 脚本 | `npm test` 可用 | `npm test` 输出 65 通过 |

> **【决策点 2】**：一次性脚本是「归档」还是「删除」？我建议**归档到 `_archive/`**（这些脚本里含 `_tmp_xunhang_full_report.txt` 等历史分析结论，可能有参考价值），评审确认后 P1-1 才执行。

### P2 — 规则单一化与性能（第二周起，视业务优先级）

| 步骤 | 具体动作 | 产出 | 验收标准 |
|---|---|---|---|
| P2-1 | 确定**唯一费率事实源**：以 `ozon-react/src/utils/wbConfig.js`（或 `ozon-product-analyzer/wb_data/tariffs.json`）为准，另一端改为读取同一份 JSON | 双端同源 | 改一次费率，React 和 Python 结果一致 |
| P2-2 | 拆分巨型组件 `NewDashboard.jsx`(459KB)、`WBCalc.jsx`(90KB) | 每文件 < 300 行 | 功能不变 + 测试仍通过 |
| P2-3 | `vite` 开启代码分割（React.lazy + 路由级分包） | dist 主 chunk 显著缩小 | 首屏加载 < 3s |
| P2-4 | 把 1201 行选品数据接入 Streamlit 评分面板，产出第一版「可测 SKU 清单」 | 业务交付物 | 输出 A/B 类推荐表 |

> **【决策点 3】**：P2-1 是「React 为源、Python 读 JSON」还是「Python 为源、React 读 JSON」？我建议 **React 端为唯一源**（因为前端是当前主用入口，且 `wbConfig.js`/`ozonEngine.js` 已有完整结构），评审可提出相反意见。

---

## 3. 明确「不做」清单（防范围蔓延）

- ❌ 不重构业务逻辑、不改计算规则（测试已过，规则按需求说明书锁定）；
- ❌ 不迁移技术栈（React/Vite、Streamlit、Node 保持不变）；
- ❌ 不接 WB/Ozon 官方 API（需求说明书 V1 明确不强制）；
- ❌ 不清理 `市场分析/`、`选品/`、`运费计算/` 里的业务数据文件（它们是有价值资产）。

---

## 4. 风险与回滚

| 风险 | 影响 | 缓解/回滚 |
|---|---|---|
| P0 先提交把垃圾也入库 | 仓库臃肿 | 已通过 `.gitignore` 排除 `_tmp_*`、`*.exe`、`node_modules`；后续可用 `git rm --cached` 补救 |
| P1-1 移动脚本破坏引用 | 某脚本被 `require` | 移动前 `grep` 确认无相对引用（这些 `debug_*` 均为独立执行，风险低） |
| P1-4 改路径导致 server.js 找不到数据 | 服务启动失败 | 保留 `E:\Desktop\坪优报价分析` 作为默认回退，环境变量优先 |
| P2-1 双端字段不一致 | 计算结果漂移 | 用现有 65 项测试 + Python `wb_test.py` 做双端对拍 |

---

## 5. 建议的评审关注点（请 ChatGPT 重点确认）

1. **【决策点 1】** P0 是先备份提交，还是先清理？我选先备份。
2. **【决策点 2】** 一次性脚本归档还是直接删除？
3. **【决策点 3】** 唯一费率事实源选 React 端还是 Python 端？
4. 是否有我遗漏的**敏感信息**（API key、内网路径）需要优先从仓库剔除？
5. P1-4 的「去 E 盘依赖」是否会破坏你本机正在使用的自动化流程（`server.js` 的文件监听依赖 `E:\Desktop\坪优报价分析`）？
