# 流程任务书 — Ozon FBP 边境仓核算（2026-09-02）

> 主题：FBP（Fulfilled by Partner）边境仓模式独立核算页面——国内段到边境仓运费 + FBP 国际段费率 + 90 天免仓期仓租 + 完整利润链
> 状态：**已上线**（commit `3cdd7d4` 已推送 origin/main；deploy run 33583444717 success；fyzsxnb.com/kuajing/ 验收通过，见 §5.2）
> 资费表来源：`运费计算/FBP_list_of_services_CN_HK1092026_1788173088.xlsx`
> 前序：《流程任务书-20260901-选品市场分析纯度流水线.md》（沿用其配置同步/测试/提交范围纪律）

---

## 0. 交接速览

| 项 | 内容 |
| --- | --- |
| 新页面入口 | 侧栏「物流与成本 → FBP 边境仓核算」（node id `__fbp_calc__`，懒加载 chunk 96.63 kB / gzip 10.55 kB） |
| 渠道配置 | `config/ozon_fbp_channels.json`（**唯一事实源**，sync 生成 `generated/ozon_fbp_channels.js`，禁止手改生成物） |
| 资费版本 | `FBP_VERSION` 从 source 动态提取 = `HK1092026`（沿袭 TARIFF_VERSION 惯例，禁止 UI 硬编码） |
| 渠道规模 | 142 条线路 / 25 分组 / 7 物流商（CEL、GUOO、RETS、Ural、Ural HK、XY、ZTO）/ 3 目的国（RU 70 / BY 42 / KZ 30）/ 11 边境仓 |
| 计费类型 | actual 实重 94 条 / vol_12000 46 条 / conditional 2 条（Ural Super Express，三边和>90cm 切体积重÷6000） |
| 仓储参数 | 90 天免仓期，其后 ¥4/m³/天，单件口径 `体积m³ × 超期天数 × 4` |
| 核心引擎 | `ozon-react/src/utils/ozonFbpEngine.js`（与 ozonEngine 并列，**不改 rFBS 引擎**） |
| 测试 | `ozonFbpEngine.test.mjs` 29 用例，挂入 `npm test` 主链（`test:fbp`） |
| 尾程费 | **一期手动输入**（₽/件，默认 0）；配置已预留 `last_mile` 结构，费率表到位后切自动查表（P5） |

业务定位：与 OzonCalc（rFBS 跨境直邮）并列的**独立核算页**，供 FBP 边境仓备货模式选品与定价对比。DEX 美元线路与 Smart 服务一期排除（方案 §3.1 已冻结）。

---

## 1. 用户口径冻结记录（重要，禁止回退）

1. **独立新页面**，不做 OzonCalc 内模式切换；挂「物流与成本」组与 rFBS 面板并列。
2. **三国全做**：目的国下拉（俄罗斯默认 / 白俄罗斯 / 哈萨克斯坦），费率按目的国分组。
3. **DEX 美元线路一期排除**（含其条件体积重规则）；**Smart 服务跳过**（费率与纯 Standard/Economy 重复）。
4. **成本模型必须含国内段**：采购成本 + 工厂→边境仓运费（¥/件）+ 贴标费。
5. **仓储费**：90 天免仓期，其后每立方每天 ¥4，按单件体积 × 库存天数摊算（库存天数默认 90）。
6. **尾程配送费一期手动输入**（默认 0，利润为不含尾程口径），UI 强标注待费率表；到位后切自动查表，**表到位前不冻结项目成本场景**。
7. 汇率沿用 ozonEngine live binding `R`/`rubPerCny`（**硬约束：不得改为 const**，见 20260826 任务书 §6.3）；代理费沿用 settings.json `agency_fee`（2%/15₽/200₽ 封顶）。

---

## 2. 架构与利润链

```text
config/ozon_fbp_channels.json ──sync-config(validateFbpChannels, fail-close)──▶ generated/ozon_fbp_channels.js
                                                                                ↓
OzonFbpCalc.jsx ──▶ ozonFbpEngine.js ──▶ ozonEngine.js（汇率 R live binding + 代理费 + round2）

单件利润链（¥）：
  成交价₽ ÷ R = 成交价¥
  − 采购成本 − 国内段运费(工厂→边境仓) − 贴标费          ← 国内段
  − FBP国际段 = fixed_cny + rate_per_g_cny × 计费重g×1000 ← 3PL段（中国揽收点→Ozon分拣中心）
      计费重 = 实重 | max(实重, L×W×H÷12000) | max(实重, L×W×H÷6000) | 条件(三边和>90cm 才用体积重)
  − 尾程配送费₽ ÷ R                                     ← 一期手动输入，默认 0
  − 仓储费 = max(0, 库存天数−90) × L×W×H/1e6 × ¥4       ← 90 天免租
  − 代理费（₽计：2%/15₽/200₽ 封顶）÷ R
  − (佣金% + 广告% + 支付费%) × 成交价¥
  − 退货损失% × 成交价¥
  = 利润 / 利润率

渠道适用性过滤（自动，不可用线路灰显带原因）：
目的国 → 边境仓(可选，筛 3PL) → 最长边/三边和 → 重量区间(g) → 申报价值区间(₽) → 电池/液体三态
（商品勾「含电池/含液体」时排除 forbidden 线路；msds 线路保留但标注提示）
```

---

## 3. 文件清单

### 3.1 新增

| 文件 | 说明 |
| --- | --- |
| `config/ozon_fbp_channels.json` | 唯一事实源：142 线路 + 11 仓映射 + storage + last_mile 预留结构（142 KB） |
| `scripts/extract_fbp_tariffs.js` | xlsx→JSON 提取：跳过 `переход` 分隔行、清洗俄式小数逗号/全角空格、per_100g 线性折算 per_g、DEX/Smart 排除、WHS 表解析仓库映射 |
| `ozon-react/src/generated/ozon_fbp_channels.js` | sync 生成物（勿手改） |
| `ozon-react/src/utils/ozonFbpEngine.js` | 引擎：`FBP_VERSION/FBP_STORAGE/FBP_LAST_MILE/FBP_DESTINATIONS/FBP_WAREHOUSES`、`filterFbpChannels`、`calcFbpShipping`、`calcStorageFee`、`calcFbpProfit`、`getBestFbpProfit` |
| `ozon-react/src/utils/ozonFbpEngine.test.mjs` | 29 用例：计费三模式、过滤六维、仓租边界、利润链复算、live binding |
| `ozon-react/src/components/OzonFbpCalc.jsx` | 独立页面：参数区（商品/国内段/平台费）/ 最优线路卡（8 项成本分解）/ 全线路对比表 / 保存历史（localStorage） |

### 3.2 修改

| 文件 | 变更 |
| --- | --- |
| `scripts/sync-config.js` | SOURCES 增 `ozon_fbp_channels.json`；新增 `validateFbpChannels`（渠道 id 唯一、计费类型枚举、体积重参数完整性、三态白名单，fail-close） |
| `package.json` | 新增 `test:fbp` 并入 `npm test` 主链 |
| `ozon-react/src/App.jsx` | lazy 引入 OzonFbpCalc + 路由分支 `__fbp_calc__` + 错误边界标题映射 + 移动端 option |
| `ozon-react/src/components/Sidebar.jsx` | 「物流与成本」组新增导航项（Warehouse 图标） |

### 3.3 不提交文件（沿袭纪律，见 20260901 任务书 §6.1）

bydCP0818.sh、fybydcp0818.html、content_production/、hwj07_audit/、reports/、tools/、scratch_*、scripts/archive_automotive_content.js、`~$FBP_*.xlsx`（Excel 锁文件）。**FBP 需求策划方案与 xlsx 源表是否入库由用户决定**（xlsx 原表 178KB，建议入库以保 provenance；`~$` 锁文件必须排除）。

---

## 4. 数据侦察结论与方案修正

- 实测分布：142 条 = RU 70 / BY 42 / KZ 30；物流商 CEL 30 / GUOO 36 / RETS 36 / Ural 21 / Ural HK 1 / XY 12 / ZTO 6；等级 Standard 78 / Economy 54 / Express 7 / Super Express 3。
- **方案修正 1**：策划方案 §2.1 预判"条件体积重仅 DEX，随 DEX 一并排除"——实际 **Ural Super Express 也有 2 条 conditional 线路**（三边和>90cm 切体积重÷6000），已保留入配置并有双边界测试（≤90cm 用实重 / >90cm 用体积重）。
- **方案修正 2**：策划方案 §5.3 预写 Python 提取脚本，实际用 Node（`extract_fbp_tariffs.js`）与现有工具链一致；输出零异常行，142 条全部通过 validateFbpChannels。
- 俄式小数（`¥0,0371`）与全角空格（`¥ 25.83 + ¥ 0.0191/1 克`）解析有单测夹具；`переход` 分隔行跳过。
- Ural HK 单条线路为 per_100g 计价，线性折算为 per_g（误差 ≤¥0.1/单，方案 §7 已接受）。

## 4.1 本次修复的 bug（开发中发现，已防回归）

1. **体积重单位错误**（关键）：初版把体积重公式结果（kg）当克用（64000cm³÷6000=10.67kg 被算作 10.67g）。修复：`calcFbpShipping` 全程 kg 口径，`cost = fixed + rate_per_g × chargeWeightKg × 1000`；单测锁定 `volumetricWeightKg` 与 `chargeWeightKg` 双断言。
2. 条件体积重解析失败：Ural Super Express 因 conditional 类型被初版 parseChargeType 跳过 → 扩展支持 `vol_threshold_sum_cm + vol_divisor`。
3. 测试夹具重量越界：CEL Big 区间下限 2001g，用例 2kg 触发超重拒绝 → 改 2.5kg。
4. 测试配置导入路径多一级目录（`../../generated` → `../generated`）。

---

## 5. 验证记录（2026-09-02）

| 步骤 | 命令 | 结果 |
| --- | --- | --- |
| 配置同步 | `node scripts/sync-config.js`（npm test 前置自动跑） | 5 配置全部生成，validateFbpChannels 通过（142 线路零异常） |
| FBP 单测 | `npm run test:fbp` | 29/29 通过 |
| 全量回归 | `npm test` | 主链全绿（sync + wbEngine + exchangeRate + scoring + t6 + market 130 + fbp 29 + scoring-golden + python 44/44 收尾） |
| 构建 | `cd ozon-react && npm run build` | 通过；OzonFbpCalc 懒加载 chunk 96.63 kB（gzip 10.55 kB），不进首屏 |
| 抽样复算 | 单测夹具取真实行 | CEL Standard Small 1000g：17.97 + 0.0393×1000 = ¥57.27 ✓；CEL Big 50×40×30 体积重 5kg 取大 ✓；仓租 150 天 0.003m³×60×4=¥0.72 ✓ |

### 5.1 部署与验收记录（2026-09-02）

- **提交**：`3cdd7d4`（feat: add Ozon FBP border warehouse profit calculator，15 文件 +9298/−7），已推送 `origin/main`。
- **CI（push）**：run 33583013290 verify success（全量测试含 test:fbp 29 用例 + golden + sync + web-persistence + PHP lint；deploy job 按 `workflow_dispatch` 条件自动跳过，符合工作流设计）。
- **CI（dispatch）**：run 33583444717 verify + deploy 双 success；FTP mirror --reverse --delete 上传 Hostinger 成功，无超时复现。
- **线上验收**：`https://fyzsxnb.com/kuajing/` 主 bundle 已切换 `index-CBhzPVQY.js`（HTTP 200，1.52MB，含 `__fbp_calc__` 路由）；FBP 懒加载 chunk `OzonFbpCalc-kGfieGQk.js` HTTP 200（108KB），内含资费版本 `HK1092026`、费率样本 `.0393`、渠道 ID（`cel_ru`）、仓库名（Xingyuan）与 `free_days` 仓储参数——142 条线路数据确认上线。
- **提交范围复核**：BYD（bydCP0818.sh / fybydcp0818.html / content_production/ / hwj07_audit/）、reports/、tools/、scratch_*、archive_automotive_content.js、Excel `~$` 锁文件均未纳入提交；manifest.json 时间戳改动未提交；FBP 资费 xlsx 源表入库（沿袭 CEL V3.31/V5.23/V7.24 先例）。
- **验收待办（用户浏览器侧）**：三国切换 / 边境仓筛选 / 含电池液体过滤 / 库存天数>90 仓租变化 / 保存参数与历史记录的交互验证。

### 5.2 待用户验收项（部署后）

- `https://fyzsxnb.com/kuajing/` 新「FBP 边境仓核算」入口加载正常（桌面侧栏「物流与成本」组 + 移动端下拉 option）。
- 三国切换（RU/BY/KZ）、边境仓筛选、含电池/含液体过滤、库存天数 >90 时仓租实时变化。
- 保存参数与最优方案记录（localStorage `fbp-calc-params-v1` / `fbp-calc-history-v1`，保留 20 条）。

---

## 6. 遗留待办（按优先级）

1. ~~提交 + 部署~~ **已完成（2026-09-02）**：commit `3cdd7d4` 已推送并上线（含 20260901 任务书部署补录一并入库），验收记录见 §5.1；用户浏览器侧交互验收待办见 §5.2。
2. **P5 尾程费率表**（阻塞依赖：待用户提供 Ozon 尾程配送费率表 + 计费口径）：填 `config/ozon_fbp_channels.json` 的 `last_mile.rows` → 引擎切自动查表 → UI 撤手动输入与琥珀色提示条 → 补测试。约 0.5 天。
3. **二期**：DEX 美元线路（需汇率折算口径）、逆向物流结构化计费（现仅文本展示）、模块1 批次摊销落地后 FBP「国内段运费」切换摊销结果引用。
4. 沿袭遗留：hPanel 清理 4 个部署残留目录；GitHub Actions FTP 超时观察项。

---

## 7. 接手人注意事项

### 7.1 配置唯一事实源纪律
费率改动**只改** `config/ozon_fbp_channels.json` → `npm run sync:config`（npm test 前置自动 sync）→ `npm test`。禁止手改 `generated/ozon_fbp_channels.js`；禁止在组件里写死费率数字或版本号（`FBP_VERSION` 动态提取自 source 的 `HK\d+`）。

### 7.2 引擎边界
- `ozonFbpEngine.js` 只做数据映射与计算，费率数值禁止在此修改；汇率走 `ozonEngine` 的 live binding（改回 const 会静默破坏每日自动更新，隐性依赖见 20260826 §6.3）。
- 体积重全程 kg 口径（rate_per_g_cny × chargeWeightKg × 1000），改单位前先跑 `test:fbp` 的 vol_12000/conditional 用例。
- `calcFbpProfit` 的 `params.agencyFee` 接受字符串/数字/空（空则回落 settings.json agency_fee.rate×100），UI 原样透传即可。

### 7.3 UI 交互约定（用户偏好）
- 参数与最优方案**手动点「保存参数与最优方案」**才持久化；历史记录保留 20 条。
- 尾程费未接费率表前，页面顶部琥珀色提示条必须保留（利润为不含尾程口径）。
- 不可用线路灰显 + 悬停原因，不做静默隐藏；MSDS 线路黄字标注。

### 7.4 测试链
`npm test` 已含 `test:fbp`。改 extract_fbp_tariffs.js / ozonFbpEngine.js / ozon_fbp_channels.json 任何一处后必须全绿再合并。

---

## 8. 相关文档索引

- 《FBP边境仓核算-需求策划方案.md》——需求冻结与口径决策（§3.1 冻结、§8 依赖清单）
- 《流程任务书-20260901-选品市场分析纯度流水线.md》——提交范围纪律（§6.1）、部署流程沿用 20260826 §4
- 《流程任务书-20260826-CEL费率V724与每日汇率自动更新.md》——live binding 硬约束（§6.3）、手动部署流程（§4）
- `CHANGELOG.md` 2026-09-02 条目
