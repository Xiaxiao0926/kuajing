# T5-0 嵌入布局审计（Embedded Workspace Layout Audit）

> T5 第一阶段前置审计：只读分析，不改业务代码。结论作为 T5-1（Workspace Shell）与 T5-2（导航 IA）的实施依据。
> 基线：`v3-t4-product-scoring`（T4 已冻结）。

---

## 1. 挂载方式：同页挂载，非 iframe

- WordPress 页面 `/kuajing/`（post_name=`kuajing`）内容为短代码 `[kuajing_dashboard]`。
- 插件 `wordpress/kuajing-persistence/kuajing-persistence.php` 的 `render_dashboard()` 输出：

```html
<script type="application/json" id="fyzsxnb-kuajing-config">…</script>
<div id="root" class="fyzsxnb-kuajing-root"></div>
```

- React（`main.jsx`）挂载到 `#root`。**没有 iframe**——React DOM 直接生长在主题页面内容容器内，React 与主题共享同一个文档与样式层。
- 运行时配置经 `#fyzsxnb-kuajing-config` 注入（`apiBase`=WP REST、`assetBase`=插件 dist、`dataBase`=GitHub raw `main/ozon-react/public/data/`、`sessionRequired`=true）。
- 资产：插件 `wp_enqueue_style/script`（script 转 module，footer 输出）；CSS 在短代码渲染时 late-enqueue（进入 footer）。

## 2. 宽度问题根因（"900px 小后台挤在页面中央"）

1. React root 位于主题的**正文内容容器**内，受主题 content max-width 约束（Hostinger 主题常规 1100–1200px，减去 padding 后正文约 900–1000px）。
2. 应用自身布局 `min-h-screen + w-64 Sidebar + flex-1 main` 没有任何"突破容器"的机制，只能被压缩成窄条。
3. FYZSXNB 站 Header 与正文之间还有主题的页头（标题区），叠加 App 内部 Sidebar 头部（"坪山综合保跨境项目"），形成三层导航堆叠。

## 3. CSS 双向污染现状

**React → WordPress（确认存在）**：
- `index.css` 顶部 `@tailwind base` 输出 Tailwind preflight（`*,::before,::after { box-sizing:border-box; margin:0; padding:0 }`、`h1-h6` 去粗体、`ul` 去圆点、`button` 透明化等）。
- 该 CSS 在 `/kuajing/` 页整页生效 → **会重置 FYZSXNB 主题 Header/Footer 的标题、段落、按钮、列表样式**。

**WordPress → React（确认存在）**：
- 主题全局样式（h1–h6 字号、table 边框、button 外观、entry-content 行高/字号）同样作用于 React DOM，与 Tailwind 类互相拉锯，靠样式顺序/特异性偶然取胜。

**已有缓解（不足）**：`.fyzsxnb-kuajing-root` 只设置了 `font-family / background / color`，没有做任何隔离。

## 4. 现状基线（App 壳层）

- `App.jsx` DashboardApp：`min-h-screen bg-morandi-bg lg:flex` → `Sidebar`（w-64 sticky h-screen，36 节点工作流 + 数据源面板 + 上传）→ `main p-3/4/6`；`<lg` 时 Sidebar 隐藏，用移动端 select 切换节点。
- 无 Workspace Topbar：项目信息只存在于 Sidebar 头（emoji 🗺️ + 标题）与各页面卡片内。
- 导航 = 7 阶段 × 36 节点流程树（`data/roadmap.js`），承担了"主导航"职责；字号普遍 9–10px。
- 访问控制：`AccessGate`（会话 cookie + WP REST /session），`sessionRequired=true` 时先出密码页。
- 评分面板当前挂载在 `MarketResearch`（NewDashboard 之后、`dashboardRef` 之外），不是独立页面。

## 5. T5-1/T5-2 修复方案（审计结论）

| 问题 | 方案 | 落点 |
|---|---|---|
| 主题容器限宽 | 全出血技巧：`body.fyzsxnb-kuajing-page` 加 body class；`.fyzsxnb-kuajing-root { width:100vw; margin-left:calc(50% - 50vw) }`，App 内部再套 `max-width:1760px` 容器 | WP 插件 `body_class` 过滤器 + `render_dashboard` 内联 scoped `<style>`（仅 /kuajing/ 页） |
| React 污染主题 | 关闭 Tailwind preflight，改为 `.fyzsxnb-kuajing-root` 作用域内的等价 reset | `tailwind.config.js` `corePlugins.preflight=false` + `index.css` scoped reset |
| 主题污染 React | Tailwind `important: '.fyzsxnb-kuajing-root'` 选择器策略，让 App 工具类稳定压过主题样式 | `tailwind.config.js` `important` 配置 |
| 三层导航 | 增加 Workspace Topbar（项目/阶段/进度/操作，64–80px）；Sidebar 改业务模块导航 + 项目进度 checklist（T5-2） | `App.jsx` + 新 `WorkspaceTopbar.jsx` + `Sidebar.jsx` 重写 |
| 字号过小 | 全局最小 12px（T5-2 导航、T5-3 全站） | 组件层 |
| 评分面板升级为一级页面 | `__scoring__` 导航节点 → 独立懒加载页面；从 MarketResearch 移除内嵌实例（PDF 隔离天然保持） | `App.jsx` + `MarketResearch.jsx` |

**风险与验证点**：
- `100vw` 出血需 `body.fyzsxnb-kuajing-page { overflow-x: clip }` 防止横向滚动条（滚动条宽度导致的 100vw 溢出）；
- WP 管理栏（登录态 32px）不影响 `margin-left: calc(50% - 50vw)` 的居中计算；
- preflight 关闭后 App 内部视觉等价性靠 scoped reset 复刻（盒模型/边距/列表/标题/表单字体继承），需人工回归各页面；
- `important` 选择器策略会使产物 CSS 体积上升（每规则多一个选择器），在 T5 交付报告记录 bundle 变化；
- 改动全部 scope 到 `.fyzsxnb-kuajing-root` / `body.fyzsxnb-kuajing-page`，禁止全局 `div/table/button` 选择器；
- dev（`index.html`）与生产（WP 插件）都要给 `#root` 加同一 class 保持一致性。

## 6. 本次审计不动的部分

评分引擎/规则/适配器、CEL、WB/Ozon 公式、config、golden、数据 schema、AccessGate 逻辑、旧 TD（TD-3/8/9/10/11/12/14/16/17/18/19）。
