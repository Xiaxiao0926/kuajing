# T5-P0 评分页白屏修复报告

> 分支：`fix/t5-scoring-blank`（自含 T5 PR#4 的 main `189ba40` 切出）
> 性质：线上功能回归（P0），T5-3/4/5 暂停。
> 模型冻结：λ=0.5、六维权重、Grade/Decision/Gate/Supply Gap/适配器/CEL/数据零改动；冻结分布 1/191/448/169/191 不变。

---

## 1. 现象与时间线

- T5 第一轮（含 T5-2 将「选品评分」改为 `React.lazy()` 一级页面）经 PR #4 合入 main 并部署；
- 生产点击「市场与选品 → 选品评分」出现空白页。

## 2. 排查（先复现，不猜；如实标注环境限制）

**本环境无浏览器，无法点击/看 Console——真实浏览器 Console 级确认需由需求方在生产复现时完成（F12 截图）。** 我能做的资产与代码级证据如下：

| 步骤 | 结果 |
|---|---|
| 本地 `npm run build` + `vite preview` 逐资产 HTTP | 入口 200；数据 JSON 200 |
| `dist/.vite/manifest.json` 结构检查 | **发现关键结构**：`ProductScoringSection-*.js`（42KB）的 import 链为 `import {...} from "./index-*.js"` + `settings-*.js` + `ozonEngine-*.js` —— **懒加载 chunk 反向依赖入口 chunk**（React/xlsx 被 Rollup 提升进入口，评分 chunk 再动态引用入口） |
| 该结构的风险 | 点击评分页时浏览器需在运行时二次拉取入口 chunk 作为模块解析；在 WordPress + LiteSpeed 缓存/`type=module` 加载环境下，这条动态加载链失败即抛 `Failed to fetch dynamically imported module`；**App 无错误边界 → React 整树卸载 → 白屏**（与现象吻合） |

结论（**最可能故障路径 / 高度怀疑，精确触发机制待生产浏览器 Console/Network 复核**）：T5-2 引入的评分页 lazy 化在 WP 生产环境失效——chunk→entry 反向引用结构本身不能单独证明必然失败，但该动态加载链一旦在 WordPress + LiteSpeed 缓存/`type=module` 环境下失败即抛 `Failed to fetch dynamically imported module`；**App 无错误边界 → React 整树卸载 → 白屏**（与现象吻合）。静态 import 是正确 P0 方案，因为它直接消除整类动态加载故障面，不依赖先把 LiteSpeed 具体行为研究透。评分模型不是故障原因——模型/适配层代码零改动，且组件内部的数据失败分支（红色初始化失败框）在白屏路径上根本没机会执行。

## 3. 修复内容

1. **保守 hotfix（已预批准）**：评分页改回**静态 import**（`App.jsx` 顶层 `import ProductScoringSection`），删除该页面 `React.lazy()`。理由：App 本身已静态 import `xlsx`，lazy 收益不足以承担线上白屏；静态 import 后评分页零运行时 chunk 请求，整类故障消除。代码分割优化单独立项。
2. **新增 `WorkspacePageErrorBoundary.jsx`**：包裹 `renderContent()` 全部页面内容（含评分路由，标题按路由映射）；懒加载失败/渲染异常/初始化异常一律进入可见错误态（错误信息 + [重新加载页面]，开发环境附 stack），**任何异常不再白屏**。Console 保留完整错误输出，不吞错。
3. **新增生产冒烟守卫 `scripts/verify-scoring-build.js`**（`npm run verify:scoring-build`）并接入 `deploy-kuajing.yml` build 后步骤：
   - manifest 入口存在、所有 chunk 的 `imports/dynamicImports` 引用产物真实存在（防 FTP mirror 缺文件部署）；
   - 两个评分数据 JSON 存在、候选行数=1000、BSR 结构完整；
   - App 路由契约：`__scoring__` 分支 + 静态 import + ErrorBoundary 包裹。
   - 任一失败 → exit 1，阻断部署。

## 4. Bundle 变化（before/after 已记录）

| 指标 | 修复前（lazy） | 修复后（静态） |
|---|---|---|
| 入口 `index-*.js` | 1227 KB | 1283 KB（+56 KB） |
| `ProductScoringSection-*.js` chunk | 42 KB | **0（已并入入口）** |
| `MarketResearch` chunk | 554 KB | 516 KB |

评分页点击不再产生任何动态 chunk 请求。

## 5. 验收

| 项 | 结果 |
|---|---|
| `npm test`（含评分适配层回归，冻结分布锁） | ✅ 1/191/448/169/191 不变 |
| `test:golden` / `test:sync` / `test:web-persistence` | ✅ 76 / 零差异 / 通过 |
| `npm --prefix ozon-react run build` | ✅ |
| `verify-scoring-build` 守卫 | ✅ 全部通过 |
| 本地 preview 资产级验证 | ✅ 入口含评分面板代码；`ProductScoringSection-*.js` 为 0 个文件（动态链已消除） |
| 生产真实点击验证 | ⏳ 需需求方在部署后 F12 + Ctrl+F5 确认（本环境无法执行） |

## 6. 生产部署与回滚

- 部署：merge 本分支后走常规 deploy（workflow 现含新守卫），或 `workflow_dispatch` 直接选 `fix/t5-scoring-blank`。
- 回滚基线修正：**在 P0 merge 之前，重新部署 `main` 不是回滚到好版本——那仍是已知会白屏的 T5 第一轮**。已知稳定基线是 T5 前的 T4 `d100aa1`；若临时部署 fix 分支后出现更严重问题，应建立临时 rollback 分支指向 `d100aa1` 再 `workflow_dispatch`，或按 Git 纪律 revert T5 merge，而不是把当时的 main 当安全回滚点。
- 数据侧说明：生产 dataBase 仍为 GitHub raw `main/public/data/`，两个评分 JSON 已在 main；若请求失败组件会显示红色初始化失败框（不再白屏）。
