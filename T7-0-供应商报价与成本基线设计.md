# T7-0 供应商报价与成本基线 数据模型设计（V1.0 冻结）

> 阶段：T7-0（只做规格）。V1.0 = 需求方确认冻结：单位成本口径 CNY（人工折算）、模具/打样费只作参考不摊销、
> T7-1 范围 = store 实体+校验+日志+测试（仿 T6-1；UI/适配器/Gate 激活留 T7-2/T7-3）。
> 前置：T6 已验收封板（tag `v3-t6-sku-project-lifecycle-hotfix1` = `8887ca1`）。本设计是 T6 的**加法扩展**：
> 激活 T6-0 预留的 `project.suppliers[]`，新增 SupplierQuote / SampleRecord 与供应方案选择，
> 使报价可直接带入 Ozon/WB CostScenario 重新测算；**不回改任何 T6 冻结内容**（实体契约、Gate 规则、
> CostScenario 记录结构、引擎公式、config 费率、评分分布）。

---

## 1. 业务目标（需求方方向，T7）

```text
市场判断 → SKU Project → 供应商 A（Quote v1 / Quote v2）｜供应商 B（Quote v1）
  → 选择供应方案（某供应商的某个报价版本）
  → 报价带入 Ozon / WB CostScenario 重新测算
  → 人工决定是否替换 Baseline
```

最终使 `project.suppliers[]` 真正激活：**供应商 → 报价版本(Quote Version) → 样品 → 成本基线**，
报价成为成本场景的输入来源之一，项目系统与真实采购工作打通。

## 2. 设计原则（沿用 T6-0 P1-P8 + T7 新增）

| # | 原则 | 含义 |
|---|---|---|
| P9 | **报价版本不可变** | SupplierQuote 是 append-only：同一供应商新报价 = 新记录 + version+1；旧报价字节永远不变（历史审计快照） |
| P10 | **供应方案是项目级引用** | `project.supply = {supplierId, quoteId}`，换方案只改引用，不修改任何报价/场景 |
| P11 | **场景可追溯到报价** | 由报价带入的成本场景在 inputPayload 内冻结 `quoteRef`（供应商+报价版本），回答"这版利润用的哪家哪版报价" |
| P12 | **样品是独立证据域** | SampleRecord 记录打样过程（下单/到样/测试/确认），关联报价但不过度耦合（样品可能先于正式报价） |
| P13 | **报价只带入已定义字段** | quote→面板 只映射 `unitPriceCny`（及后续明确字段），绝不虚构/绝不自动改佣金与费率假设（沿用 T6-2B 规则） |

## 3. 实体总览与关系（V0.1）

```text
SkuProject（T6，不变）
  ├── suppliers: string[]            Supplier.id 引用（append-only，激活）
  ├── samples: string[]              SampleRecord.id 引用（append-only，激活）
  ├── supply: { supplierId, quoteId, selectedAt, selectedBy, note } | null   （T7 新增字段）
  ├── costing.scenarios[]            CostScenario.id（T6，不变）
  └── costing.baselineScenarioId     （T6，不变；T7 仍人工切换）

Supplier ──< SupplierQuote（版本不可变：v1/v2/…；(supplierId, version) 唯一）
   │
   ├── SampleRecord（打样：状态机 ordered→arrived→tested→approved|rejected；可关联 quote）
   │
   └── 被选择为 supply 后：
          quote.unitPriceCny → Ozon/WB 面板 purchaseCost（其余假设仍人工确认）
          → 保存 CostScenario（inputPayload 冻结 quoteRef）
          → 人工决定是否替换 baseline
```

## 4. 实体定义

### 4.1 Supplier（供应商；名称/联系方式可编辑，报价只增）

```ts
interface Supplier {
  id: string                     // uuid；t6.supplier.<uuid>
  projectId: string              // 归属项目（fail-close：项目必须存在）
  name: string
  contact: string
  quotes: string[]               // SupplierQuote.id 引用（append-only，系统追加）
  notes: string
  createdAt: string              // 不可修改
  updatedAt: string              // 名称/联系方式/备注修改时更新
}
```

- 删除策略：**不提供 delete**（已有报价/被选择为 supply 的供应商不可删）；仅有零报价且未被引用的供应商可在 UI 隐藏（不做物理删除，登记为决策）。

### 4.2 SupplierQuote（报价版本，不可变）

```ts
interface SupplierQuote {
  id: string                     // uuid；t6.supplierQuote.<uuid>
  supplierId: string             // 归属供应商（必须属于某项目）
  version: number                // 供应商内序号：1,2,3…；append 时 = max+1；(supplierId, version) 唯一
  unitPriceCny: number           // 报价单价（CNY）—— 唯一自动带入 CostScenario 的报价字段
  currency: 'CNY'|'RUB'|'USD'    // 供应商原始报价币种（存档来源；unitPriceCny 恒为 CNY 口径）
  moq: number                    // 起订量（参考信息，不进公式）
  exw: boolean                   // 工厂交货
  fob: boolean                   // 离岸价
  packagingSpec: string          // 包装规格（参考信息）
  toolingFeeCny: number|null     // 模具费（参考信息；不进单位成本，登记决策见 §9 风险 2）
  sampleFeeCny: number|null      // 打样费（参考信息；可关联 SampleRecord 记录）
  leadTimeDays: number           // 交期
  paymentTerms: string           // 付款条件
  remark: string
  createdAt: string              // 不可修改
}
```

- 不可变：创建后全字段禁改（append-only）；版本号由 store 计算，payload 禁带 `version`（防伪造/冲突）。

### 4.3 SampleRecord（样品记录，打样证据域）

```ts
interface SampleRecord {
  id: string                     // uuid；t6.sample.<uuid>
  projectId: string
  supplierId: string             // 打样供应商（必须属于项目）
  quoteId: string | null         // 可关联某个报价版本（打样费来源）；也可能先于报价存在
  status: 'ordered'|'arrived'|'tested'|'approved'|'rejected'
  trackNo: string
  arrivedAt: string | null
  testResult: string
  notes: string
  createdAt: string              // 不可修改
  updatedAt: string              // 状态/结果更新
}
```

- 与 workflow 挂点：`roadmap-v1` phase-2 n7「样品打样确认」——n7 完成（done）建议以"至少 1 个 approved 样品"为数据前提（T7 实现时做 Gate/提示联动，本阶段只定义映射）。

### 4.4 SkuProject 新增字段（加法，不动既有字段）

```ts
// 在既有 SkuProject 上新增：
supply: {
  supplierId: string
  quoteId: string
  selectedAt: string
  selectedBy: string
  note: string
} | null                        // 项目当前供应方案（项目级引用，人工可换）

// 既有字段激活语义：
suppliers: string[]             // Supplier.id（append-only）
samples: string[]               // SampleRecord.id（append-only）
```

- `supply` 校验：`quoteId` 必须属于 `supplierId`，且 supplier 必须属于本项目；引用型变更，不改报价字节。

### 4.5 CostScenario 的报价追溯（T6 记录结构不变，inputPayload 内加字段）

- **记录外层结构保持 T6 冻结**：`{id, projectId, platform, name, sourceSnapshotId, inputPayload, resolvedConfig, outputPayload, createdAt}` —— 不改 createCostScenario 契约。
- T7 适配器（`supplierScenarioAdapter.js`，T7-1 实现）在组装 inputPayload 时**在内部**追加：
  ```ts
  inputPayload.quoteRef: { supplierId: string; quoteId: string; quoteVersion: number } | null
  ```
- 冻结规则（沿用 T6-2B P1 修复后的 `mergeTrustedPrefill` 语义）：
  - `purchaseCost ← quote.unitPriceCny`（Ozon 与 WB 面板同样带入）；
  - 其余成本/费率/佣金假设**绝不**由报价自动改（人工确认 Gate 仍生效：未勾选「已确认成本与费率假设」不能保存场景）。

## 5. 流程（T7 实现后）

```text
[供应链 Tab]
1. 新增供应商 A / B（Supplier）
2. A 新增报价 v1（version=1）→ 再报价 v2（version=2，v1 字节不变）
   B 新增报价 v1
3. 打样：对 A 记录 SampleRecord（报价 v1 打样费）→ 到样 → 测试 → approved（n7 完成依据）
4. 选择供应方案：supply = { supplierId: A, quoteId: A-v2 }
   （换方案 = 改 supply 引用；旧方案引用的报价/场景全部保留）
5. 成本 Tab：[从供应方案带入报价] → Ozon/WB 面板 purchaseCost = A-v2.unitPriceCny
   → 用户确认其余假设（确认 Gate）→ 保存 CostScenario（inputPayload.quoteRef 冻结）
6. 人工决定是否替换 baseline（沿用 T6 规则：首次自动基线、其后仅人工）
7. Gate：COSTING→SAMPLING 的 supplier_quote 检查激活（见 §6）
```

## 6. Stage Gate 更新（T7 激活 supplier 检查；margin 规则不变）

| 检查 | T6-2B 现状 | T7 激活后 |
|---|---|---|
| `supplier_quote`（COSTING→SAMPLING） | NOT_EVALUATED（供应商未实现） | 0 报价 → **FAIL**；有报价但未选供应方案 → **WARN**（"报价未选定供应方案"）；已选 supply → **PASS** |
| `margin_warning`（≥15%） | 真实基线毛利判定（T6-2B1） | 不变（仍用 baseline scenario 毛利） |
| 其它未实现域（samples/compliance/listing/…） | NOT_EVALUATED | 样品域激活后 `samples_record` 转为真实检查（0 样品→FAIL；≥1 approved→PASS），其余保持 NOT_EVALUATED |

- 原则延续：Gate 只建议 + 人工 override（YELLOW/RED 必填理由）；非法阶段 fail-close 不变。

## 7. 存储（沿用 T6-0 §7 逐实体同构扩展，T6-0 已预留）

```text
t6.supplier.<uuid>        Supplier
t6.supplierQuote.<uuid>   SupplierQuote（append-only，禁系统字段注入、key 冲突 throw —— 同 CostScenario 模式）
t6.sample.<uuid>          SampleRecord
t6.project.<uuid>         增加 supply 字段（读旧数据无该字段 = null，兼容迁移）
t6.costScenario.<uuid>    记录结构不变；quoteRef 只出现在 inputPayload 内部
t6.log.<uuid>             DecisionLog 新增 kind：supplier_create / quote_create / sample_change / supply_plan_change
```

- 所有 T7 读写走 `t6Store`（扩展同一 store，禁止组件直写 persist）；UUID 主键；`schemaVersion` 仍为 1（加法字段兼容）。

## 8. 字段权限矩阵（新增行；T6 行不变）

| 实体 / 字段 | 自动带入 | 人工编辑 | 不可修改 | append-only |
|---|---|---|---|---|
| Supplier.id/projectId/createdAt | ✅ | — | ✅ | — |
| Supplier.name/contact/notes | — | ✅ | — | — |
| Supplier.quotes | ✅（报价时追加） | — | ✅（仅系统追加） | ✅ |
| SupplierQuote.全部 | ✅（创建时冻结） | — | ✅ | ✅ |
| SupplierQuote.version | ✅（store=max+1） | — | ✅（payload 禁带） | — |
| SampleRecord.status/result/notes | — | ✅（状态机内） | — | — |
| SampleRecord.id/projectId/supplierId/createdAt | ✅ | — | ✅ | — |
| SkuProject.supply | — | ✅（人工选择供应方案） | — | —（引用可换） |
| SkuProject.suppliers/samples | ✅（追加引用） | — | ✅（仅系统追加） | ✅ |
| CostScenario.inputPayload.quoteRef | ✅（保存时冻结） | — | ✅ | ✅ |

## 9. 风险与待决（如实登记）

**已确认（V1.0）**：
1. **报价币种**：`currency` 仅存档来源；`unitPriceCny` 恒为 CNY 口径，录入报价时由用户人工折算（USD/RUB→CNY 不做自动换算；若未来需要，登记 TECH_DEBT 另立）。
2. **模具费/打样费**：`toolingFeeCny`/`sampleFeeCny` 只作参考信息，不摊入单位成本（摊销规则未定义，不立项）。

**待决（不阻塞 T7-1，登记）**：
3. **MOQ 与成本测算**：MOQ 只记录不参与公式；项目数量 < MOQ 时的单价谈判语义未定义。
4. **样品域范围**：本阶段样品 = 打样确认证据；合规送检（认证样品）属 compliance 域，不混入。
5. **供应商删除**：不提供物理删除；零引用供应商 UI 隐藏（决策记录）。
6. **T6 冻结边界**：本设计不改 CostScenario 记录结构 / Gate 既有检查语义（margin 判定）/ 引擎 / config / 评分分布；全部为加法扩展。

## 10. T7-0 验收（本阶段）
- 本规格文档 V1.0 经需求方确认冻结（2026-08-14）。
- **不写代码、不改引擎/config、不建实现分支**（T7-1 起按 §11 开 `feat/v3-t7-1-*`）。

## 11. T7 阶段划分建议（T7-0 只冻结设计，供规划）
- **T7-1**：store 实体（Supplier/SupplierQuote/SampleRecord/supply）+ 引用一致性校验 + DecisionLog 新 kind + 测试（仿 T6-1）。
- **T7-2**：`supplierScenarioAdapter`（quote→inputPayload.quoteRef + mergeTrustedPrefill 复用）+ Gate supplier_quote 激活 + 成本 Tab「从供应方案带入报价」。
- **T7-3**：供应链 Tab UI（供应商/报价版本列表/选择供应方案/样品记录）+ workflow n7/n8 联动提示。
