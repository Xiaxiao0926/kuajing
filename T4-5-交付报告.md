# T4-5 Export / Explanation / Documentation / Final Regression 报告

> T4 收尾阶段：不研究模型，只做可交付化。λ=0.5、六维权重、评级线、Supply Gap、Gate/Decision 全部未动；未触碰 Git 历史。

---

## 1. 选品结果导出（XLSX 优先 + CSV）

- 新增 `ozon-react/src/utils/scoring/scoringExport.js`（纯函数，可测试）：`buildExportRows/buildExportDocuments/rowsToCsv/rowsToXlsx/exportFilename`。
- **导出对象 = 当前筛选结果**（按钮上实时显示行数），不是固定 1000 行。
- 20 列：商品名 / 类目 / 综合分 / 等级 / 暂定评级 / Decision / 下一步动作 / Context / Evidence / 市场需求 / **市场规模 / 候选相对表现** / 竞争机会 / 价格空间 / 利润可行性 / 物流适配 / 运营稳健 / Supply Gap / Gap 信号 / 风险标记。**不含内部调试字段**（无 sub 明细/原始 canonical 池）。
- 面板筛选栏新增两个按钮：`导出 XLSX（N）`、`导出 CSV（N）`（CSV 带 BOM，Excel 中文兼容；文件名 `选品评分-YYYYMMDD-HHmm.xlsx/csv`）。
- **回读验证**：`scoringExport.test.mjs` 用真实数据筛选出 60 行（B 且 ELIGIBLE）→ 写 XLSX → `xlsx` 库回读 → 行数一致、抽样 3 行 8 个关键字段逐一相等；CSV 行数=61、表头 20 列、特殊字符转义逻辑就位。**22/22 全绿**，已并入 `test:scoring` 链。

## 2. 解释完整性

- **列表简版原因**：排名表新增"简版原因"列（`briefReason`）：物流不可行·禁止采样 / 毛利为负·核实成本 / 命中合规词·人工复核 / 无市场基准·补充调研 / 关键数据不足·暂不评级。纯标签映射（输入是引擎已算出的 `status` 数组），**未新增任何判断规则**。
- **详情完整解释**：继续直接消费 `buildExplanations()` 的 strengths / risks / missingMetrics；五个 flag 的人话文案由该模块统一输出（如"毛利率为负（MARGIN_RISK），利润维度封顶处理"、"缺少对应 BSR 市场基准，评级为暂定，建议先补市场研究"、"无可用 CEL 渠道，当前跨境物流不可行"）。

## 3. 配置防护补齐（final hardening，非改模型）

`scripts/sync-config.js` → `validateScoringRules()`：
- `0 < scale_weight <= 1`（此前只校验 >0，`λ=1.5` 也能通过——现已 fail-close）；
- 六维权重之和必须 = 100（当前 25+15+10+20+15+15=100 通过）。
- 校验失败 → 同步/构建直接退出码 1，不产生部分写入。

## 4. 最终文档

| 文档 | 新增 |
|---|---|
| `ARCHITECTURE.md` | §4 增"选品评分系统（T4）"文件索引；新增 **§6 选品评分系统数据流**（xlsx→canonical→JSON→adapter→ScoredProduct→面板/导出，含审计同源与 fail-close 标注） |
| `BUSINESS_RULES.md` | 新增 **§12 选品评分决策模型（T4 V1，冻结）**：六维权重、demand 两层公式、评级线、五个 Gate、Decision 层、Supply Gap、证据感知、修改纪律 |
| `RUNBOOK.md` | 新增 **§9 选品数据更新与重建**：候选 xlsx 更新流程、BSR 基准重建、规则修改纪律、测试入口、λ 实验环境变量、UI↔审计对拍 |
| `CHANGELOG.md` | V3 段顶部新增 T4 总条目（T4-0→T4-5 各阶段一句话 + 报告链接） |
| `TECH_DEBT.md` | 新增 **TD-19**（业务数据公开化：源 xlsx/JSON/BSR 明细已在 public GitHub 含历史；彻底清除需历史重写+force push，人工审批 Gate，禁止代理执行；建议立 Data Governance / Private Data Migration 项目）。未顺手修任何旧 TD |

## 5. 最终冻结验收清单

| 验收项 | 结果 |
|---|---|
| unit 81+ | ✅ 81/81 |
| monotonicity 20 | ✅ 20/20 |
| scoring golden 56 | ✅ 56/56 |
| adapter regression 13 | ✅ 13/13（分布锁定 1/191/448/169/191） |
| scoring export 22（XLSX 回读） | ✅ 22/22 |
| 原业务 golden 76 | ✅ 76/76 |
| sync zero diff | ✅ 16 边界 + 2 版本 + scoring_rules 一致性 |
| web persistence | ✅ |
| build | ✅（vite build 通过） |
| 1000 SKU 分布 | ✅ A1/B191/C448/D169/不可评级191；LMC 均分 54.7、mapped 61.0（与 T4-4A 冻结态逐位一致） |
| 5 SKU UI/audit 对拍 | ✅ 5/5 逐位一致 |
| XLSX 导出回读验证 | ✅（§1） |
| 浏览器 smoke | ✅ 生产构建 preview：index/两个评分数据文件 HTTP 200、懒加载 chunk 含面板与导出代码；环境无 headless 浏览器，最终渲染以你本地肉眼确认（同 T4-4B 口径） |

## 6. 交付与边界

- 未改 λ=0.5、六维权重、评级线、Supply Gap、Gate/Decision；leader-share 未入分；无 AI 推荐；无视觉换皮；**未触碰 Git 历史**（TD-19 仅登记）。
- 新增：`scoringExport.js` + `scoringExport.test.mjs`；修改：`ProductScoringSection.jsx`（导出按钮/简版原因列）、`sync-config.js`（校验）、`package.json`（test 链）、五份文档。
