# 流程任务书 — 选品市场分析·纯度流水线（2026-09-01）

> 主题：竞品同类性分层 + 规格标准化价格折算 + 三维加权价格带 + 数据可信度抽检（模块 2/3/4/5 一期交付）
> 状态：**已上线**（commit `85dfa5c` 已推送 origin/main；GitHub Actions run 33478718108 success；fyzsxnb.com/kuajing/ 验收通过，见 §5.2）
> 前序：《流程任务书-20260826-CEL费率V724与每日汇率自动更新.md》（本任务沿用其部署流程 §4）

---

## 0. 交接速览

| 项 | 内容 |
| --- | --- |
| 新页面入口 | 侧栏「市场与选品 → 选品市场分析」（node id `__purity_analysis__`，懒加载） |
| 数据源 | 顶部/市场调研页已上传数据，或页内选择 public/data 热销 xlsx |
| 核心引擎 | `ozon-react/src/utils/marketAnalysis/`（4 个纯函数模块） |
| 规则配置 | `config/market_analysis.json`（**唯一事实源**，sync 生成 `generated/market_analysis.js`） |
| 测试 | specParser 36 + purityFilter 45 + pipeline.e2e 49，已挂入 `npm test` 主链（`test:market`） |
| 真实数据验证 | 发膜 380 行（A=296/B=9/C=10/UNKNOWN=65，覆盖率 82.9%）；手套 1000 行（A=774/B=12/C=12/UNKNOWN=202，覆盖率 79.8%） |

业务定位（用户拆线决策）：模块 2–5 构成**选品市场分析链**（上传→规格识别→纯度分层→标准折算→三维价格带→可信度监控）；模块 1 批次摊销属**成本核算链**，二期独立实现，不与市场解析代码耦合。

---

## 1. 用户口径冻结记录（重要，禁止回退）

1. **三维名称固定**：`SKU占比 / 销量占比 / 销售额占比`。现有数据实际字段是 `月销售额(₽)/sales_rub_28d`，**没有 GMV 字段，禁止在 UI 写 GMV%**。以后真拿到 GMV 再切换。
2. **UNKNOWN 独立成桶**：规格解析失败的商品**禁止自动塞入 C**（防"100% 分类完成"的假精确）。UNKNOWN 单独计数、单独展示成因、可进入抽检。
3. **A/B/C 用可配置规则引擎**（不是简单品类×功能）：规则全部在 `config/market_analysis.json`，按类目配置 detect_keywords / target_basis / pair_handling / implicit_singular / tier_a / tier_b / normalize。
4. **价格带进入规则**：A = 默认口径；A+B = 可切换参考口径；C 与 UNKNOWN 一律排除；占比分母随口径切换变化。
5. **实施优先级**：一期 3（规格标准化）→ 2（纯度分层）→ 4（价格带）→ 5（可信度）；二期 1（批次摊销）。

---

## 2. 架构与数据流

```text
A. 选品市场分析链（本次交付）
   上传 Ozon 热销数据
     ↓ specParser.parseSpec      标题 → 数量+单位候选（пар/шт/мл/л/кг/г，排除尺寸对/型号/功能描述陷阱）
     ↓ purityFilter.detectCategory 全量标题投票 → 类目（份额 ≥ 30% 才认定）
     ↓ purityFilter.classifyRow   A / B / C / UNKNOWN 分层 + 规格归一
     ↓ priceBands.buildWeightedBands  标准化可比价（每100ml/每100只/每只）× SKU/销量/销售额 三维占比
     ↓ credibility.buildCredibility + createSample  覆盖率 + 种子化随机 50 SKU 抽检
     → 输出"真实可比价格带"（能回答：主流价是哪批同类商品算的、规格是否统一、覆盖率、抽检准确率）

B. 成本核算链（二期，未开工）
   批次国内物流总费用 ÷ 该批实际摊销数量 = 单位国内物流成本 → Ozon/WB/SKU Project 成本场景
```

分类顺序（`classifyRow`，顺序敏感，改动前先读 purityFilter.js 头注释）：
类目未配置→UNKNOWN；пар+pair_handling=exclude→C（先于规格失败判定）；规格不可解析→UNKNOWN；tier_a.any 命中且 none 未命中→A；none 命中（套装降级）或 tier_b.any→B；其余已解析→C。

---

## 3. 文件清单

### 3.1 新增

| 文件 | 说明 |
| --- | --- |
| `config/market_analysis.json` | 规则引擎唯一事实源：5 个类目（hair_mask / hair_spray / gloves / pillows / hair_dryer）+ sampling 参数 |
| `ozon-react/src/generated/market_analysis.js` | sync 生成物（勿手改） |
| `ozon-react/src/utils/marketAnalysis/specParser.js` | 模块3：规格解析。西里尔边界用 `(?![а-яёa-z0-9])` 替代 `\b`（JS 正则 `\b` 对西里尔无效）；型号排除含大写 `[а-яёА-ЯЁa-zA-Z]+-?\d+`；词干变体如 `мас(?:ел\|л[а-яё]*)` |
| `ozon-react/src/utils/marketAnalysis/purityFilter.js` | 模块2：类目检测 + A/B/C/UNKNOWN 分层 + 规格归一（пар×2 折算、打包数量×单位容量相乘、implicit_singular 单数词形推断） |
| `ozon-react/src/utils/marketAnalysis/priceBands.js` | 模块4：排序分箱五等分位价格带 + 三维占比 |
| `ozon-react/src/utils/marketAnalysis/credibility.js` | 模块5：覆盖率统计 + mulberry32 种子化抽样 + 核验汇总（key = `seed:rowId`，rowId = 标题+价格哈希） |
| `ozon-react/src/utils/marketAnalysis/specParser.test.mjs` | 36 用例，夹具全部来自真实热销表标题 |
| `ozon-react/src/utils/marketAnalysis/purityFilter.test.mjs` | 45 用例，覆盖整条纯度流水线 |
| `ozon-react/src/utils/marketAnalysis/pipeline.e2e.test.mjs` | 49 断言，读 public/data 真实 xlsx 跑 UI 同款完整链路，并锁定移动端入口 |
| `ozon-react/src/components/PurityPipelinePage.jsx` | UI 页面：类目检测卡 / 分层分布 / 三维价格带（A/A+B 切换）/ 可信度与抽检（核验需手动保存，localStorage 持久化 + 生成记录日志） |
| `scripts/data_recon.py` | 数据侦察工具（列名/单位分布/标题陷阱统计，规则校准的 provenance） |

### 3.2 修改

| 文件 | 变更 |
| --- | --- |
| `scripts/sync-config.js` | SOURCES 增 market_analysis.json；新增 `validateMarketAnalysis`（basis 白名单 / tier 规则结构 / normalize.per>0 校验，fail-close） |
| `package.json` | 新增 `test:market` 并入 `npm test` 主链 |
| `ozon-react/src/App.jsx` | lazy 引入 PurityPipelinePage + 路由分支 `__purity_analysis__` + 错误边界标题映射 |
| `ozon-react/src/components/Sidebar.jsx` | 「市场与选品」组新增导航项（Layers 图标，位于市场调研与选品评分之间） |

---

## 4. 数据侦察结论（规则为什么长这样）

- 7 份热销表 2780 行：手套类 66.8% 标题含 пар（双），发膜类 83.7% 含 мл——所以 pair_handling 与 volume 口径是类目级配置而非全局。
- 标题陷阱（specParser 已逐一排除并有测试夹具）：尺寸对 `50x70`、型号 `AHD51`、功能描述 `для ногтей`/`масел` 词干变体、`№8`/`Nв1`/版本号、`партия`（批次）内嵌 пар 不误判。
- e2e 实测覆盖率：发膜 82.9%、手套 79.8%；UNKNOWN 主要成因 = 复数词形无数量（俄语语法省略）与非目标单位。手套 UNKNOWN 202/1000 是下一个规则增强点（见 §6.2）。

---

## 5. 验证记录（2026-09-01）

| 步骤 | 命令 | 结果 |
| --- | --- | --- |
| 配置同步 | `node scripts/sync-config.js` | 5 个配置全部生成，market_analysis 校验通过 |
| market 单测 | `npm run test:market` | 36 + 45 + 49 = 130 断言全绿 |
| 全量测试 | `npm test` | 主链全绿（wbEngine 44/44、T6 系列、scoring-golden 56/56、market 130） |
| 构建 | `cd ozon-react && npm run build` | 通过；PurityPipelinePage 懒加载 chunk 34.56 kB（gzip 11.58 kB），不进首屏 |
| 真实数据 | pipeline.e2e | 见 §0 表；主流价格带：发膜带1 中位 ₽143.8/100ml；手套带2 中位 ₽25.4/100只 |

### 5.1 本次修复的 bug（e2e 发现，已防回归）

`priceBands.js` 初版**分箱边界用排序数组、成员聚合却切未排序数组**，导致中位价/占比与价格区间错位（手套带1 区间 ₽0.5–1.0 却中位 ₽31.2）。修复：先按可比价排序整行再分箱（边界与成员同一有序序列）。e2e 新增两条守门断言：各带中位价 ∈ [priceMin, priceMax]；相邻带区间单调不重叠。另将排除明细键 `A_no_price` 改名 `no_price`（A+B 口径下原名误导）。

### 5.2 部署与验收记录（2026-09-01，用户执行）

- **提交**：`85dfa5cf23a8630e4b0be345dc79a68d6d27ef7f`（feat: add Ozon market purity pipeline，17 文件 +2383/−2），已推送 `origin/main`。
- **CI**：GitHub Actions run 33478718108 → success（20260826 遗留的 FTP 部署超时本次未复现，降级为观察项）。
- **线上验收**：`https://fyzsxnb.com/kuajing/` 新「选品市场分析」模块正常加载；生产资源返回 200，无 "assets are not deployed" 回退提示；桌面与移动入口均存在、页面无横向溢出。
- **口径验证**：A 默认、A+B 切换、C/UNKNOWN 排除、三维占比口径均已在线上验证。
- **回归**：测试 130 项通过，7 份真实 Ozon 数据表完成回归；本地预览服务已停止。
- **提交范围复核**：BYD（bydCP0818.sh / fybydcp0818.html）、reports/、tools/、content_production/、hwj07_audit/ 及 manifest.json 时间戳改动均未纳入提交（20260826 任务书 §6.5 纪律执行到位）。
- 存量 JSX 构建警告与本任务无关，未擅自修改。

---

## 6. 遗留待办（按优先级）

1. ~~commit + 部署~~ **已完成（2026-09-01）**：commit `85dfa5c` 已推送并上线，验收记录见 §5.2。BYD 无关文件未混入提交（工作区仍保留 bydCP0818.sh / fybydcp0818.html / content_production/ / hwj07_audit/ / reports/ / tools/ 未跟踪状态，沿袭 20260826 任务书 §6.5 禁止提交）。
2. **UNKNOWN 覆盖率增强**：手套 202/1000 偏高。方向：复数词形无数量的隐式推断需谨慎（有假精确风险，建议先人工抽检评估样本再决定）；新增类目按 §7.2 步骤接入。
3. **模块1 批次摊销**（二期）：进成本模型（T6 costScenario 体系），独立于 marketAnalysis 代码。
4. 沿袭遗留：hPanel 清理 4 个部署残留目录；GitHub Actions FTP 超时——本次部署（run 33478718108）未复现，降级为观察项，再次超时再修。
5. 本任务书 §5.2 与 CHANGELOG 的部署补录为本地未提交变更，需随下次 docs commit 一并入库。

---

## 7. 接手人注意事项

### 7.1 配置唯一事实源纪律
规则改动只改 `config/market_analysis.json` → `npm run sync:config`（或直接 `npm test`，test 前置自动 sync）。禁止手改 `generated/market_analysis.js`。sync 内置结构校验（fail-close）。

### 7.2 新类目接入步骤
1. `config/market_analysis.json` 加类目：label / detect_keywords（检测词，建议用词干）/ target_basis（pcs|volume_ml|weight_g）/ pair_handling / tier_a{any,none} / tier_b{any} / normalize{per,label}；
2. 类目为 pcs 且标题常省略数量时：评估 implicit_singular + singular_forms/plural_forms（俄语词法，单词形=1 属"推断"非默认值填充，UI 会标注）；
3. `npm run sync:config` → 在 purityFilter.test.mjs 补该类目用例 → `npm test`。

### 7.3 分类纪律
UNKNOWN ≠ C。任何"解析不出来的就扔 C"的捷径都会制造假精确，直接违反用户冻结口径（§1.2）。抽检面板就是用来量化 UNKNOWN 与判定质量的，不要绕过。

### 7.4 测试链
`npm test` 已含 `test:market`（三个测试文件，缺一不可）。改 specParser/purityFilter/priceBands/credibility 任何一行后必须全绿再合并；e2e 依赖 public/data 真实 xlsx，若数据文件更名需同步改 e2e 的 runDataset 入参。

### 7.5 UI 交互约定（用户偏好）
抽检核验结果必须**手动点「保存核验结果」**才持久化（localStorage key `purity-checks-v1`）；每次生成样本自动记入生成记录（key `purity-sample-history-v1`，保留最近 20 条）。汇率沿用 live binding `R`（勿改回 const，见 20260826 任务书 §6.3）。

---

## 8. 相关文档索引

- 《流程任务书-20260826-CEL费率V724与每日汇率自动更新.md》——部署流程（§4）、live binding 约束（§6.3）、勿提交文件清单（§6.5）
- `CHANGELOG.md` 2026-09-01 条目
- `scripts/data_recon.py`——数据侦察 provenance
