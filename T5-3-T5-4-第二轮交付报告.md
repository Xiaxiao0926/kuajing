# T5-3 + T5-4 第二轮交付报告

> 分支：`feat/v3-t5-visual-system`（自 main `8a04e1b` 切出）
> 性质：纯前端视觉系统 + 信息层级 + 评分 Workspace 展示重构。模型/数据/适配层零改动。
> 本轮停止 Gate：不进入 T5-5，不 merge main。

---

## 1. Before（线上基线问题清单）

1. WP「跨境运营工具」Hero 级标题占据首屏最强注意力；
2. Header → 巨型标题 → Topbar 层级过重；
3. Sidebar/Topbar/主内容字号偏小；
4. 视觉仍继承旧 Morandi（紫/pastel/AI 卡片后台）；
5. 卡片过多、圆角/浅色块/小徽标过多；
6. 评分页像数据调试工具；Decision/Context/Risk 没有形成比 Score 更强的决策层级；
7. 1000 SKU 表格阅读效率低。

## 2. After 结构

- **T5-3**：设计 token V2（Enterprise Neutral + Blue Accent）→ UI primitives（16 个）→ Sidebar/Topbar 视觉整理 → WP 标题 Hero 压缩。
- **T5-4**：评分页拆分为 Header / Overview（决策 KPI）/ Toolbar（搜索第一入口）/ Table（决策列序）/ Detail Drawer（右侧 440px overlay），Decision 视觉优先于 Score。

## 3. 设计 Token（V2，全部挂在 `.fyzsxnb-kuajing-root` 作用域）

```css
--workspace-bg #F7F8FA · --surface #FFFFFF · --surface-subtle #FAFBFC
--border #E5E7EB · --border-strong #D7DBE2
--text #17202A · --text-secondary #667085 · --text-tertiary #98A2B3
--primary #315EFB / --primary-soft #EEF3FF
--success #138A5B / soft #ECFDF3 · --warning #B7791F / soft #FFF8E7
--danger #D92D20 / soft #FEF3F2 · --info #2563EB
```

旧 `morandi-*` 变量名保留但数值 remap 到新调色板（旧紫/pastel 视觉废止，组件无需逐个改类名）。

## 4. Typography

- root 14px；数字 `tabular-nums`；Sidebar/Topbar 13px；KPI 数值 22px；表格 13px；辅助 12px；无 <12px 业务主内容（旧页面残留的 9-10px 属 T5-5 收尾范围，本轮未触碰）。
- 标题：PageHeader 24px/600；WP 页面标题 28px/650（插件 0.2.4 scoped CSS）。

## 5. Sidebar / Topbar

- Sidebar：232px 保持；品牌区 64px；一级导航 40px、二级 36px；active = 浅蓝背景 + 左侧 2px primary indicator + 深色文字；底部仅「数据源 / 项目执行进度」两块（代码中本就各渲染一次，线上截图所见重复为旧版部署缓存，本轮视觉整理后确认单实例）。
- Topbar：60px；扁平 label/value；无卡片化字段；进度/时间 `tabular-nums`。

## 6. 评分页（T5-4）

- 页面头：`选品评分` + `俄罗斯市场 · 1000 个候选 SKU · 规则 V1` + [CSV/XLSX 导出当前筛选]；规则/λ/耗时移到底部 meta 行（不在主视觉）。
- KPI 区：候选 SKU 1000 / 可执行（绿）/ B 级机会 / 高·中市场置信 / 待复核（REVIEW+HOLD）/ 不可评级 + 紧凑分布 `A 1 · B 191 · C 448 · D 169` + 决策概览一条线。
- Toolbar：SearchInput（320px）第一入口 + 等级/Decision/Context/Gap/风险/类目 + `N / 1000 行`。
- 表格列序：商品 → 分数(Score+Grade 合并) → **Decision** → 市场规模 → 候选表现 → 竞争 → 利润 → 物流 → Gap → Context → 风险（最多 2 个 +N）；行 52px；sticky header；hover 浅灰蓝；selected 左侧 primary 指示条。
- Drawer：440px 右侧 overlay（ESC / X / 遮罩关闭）。结构：商品+类目+BSR → 总分+Grade+**Decision（视觉优先）**+Evidence+Context → 市场需求（市场规模/候选相对表现/综合需求，**双分量保留**）→ 商业可行性五维统一蓝色横条（N/A 显示 `—`，tooltip"证据覆盖不足未参与评分"）→ Supply Gap → 为什么值得做（CheckCircle2）/ 主要风险（AlertTriangle，全部来自 `buildExplanations`）→ 市场基准（BSR 类型/样本 n/P25/P50/P75/Context）→ 下一步。LMC 显示"评级暂定"徽标，解释文案来自现有 risks 输出。
- Decision 映射：可推进(绿)/补市场数据(蓝)/合规复核(黄)/暂缓(橙)/阻塞(红)；**85A+BLOCKED 显示红色"阻塞"，绝不呈现为绿色推荐**；英文入 tooltip。
- 默认排序契约不变（Decision → 综合分 → Evidence）。

## 7. 文件变化

- 新增：`components/ui/`16 个 primitives；`components/scoring/`10 个展示组件；
- 重写：`ProductScoringSection.jsx`（编排层，数据逻辑逐字保留 + 新增搜索筛选）；`Sidebar.jsx`、`WorkspaceTopbar.jsx`（视觉整理）；
- 修改：`tailwind.config.js`（token remap）、`index.css`（token V2/typography/tabular-nums）、`kuajing-persistence.php`（0.2.4：标题压缩 + min-height）、`scripts/verify-scoring-build.js`（T5-4 UI 契约）。

## 8. Bundle

| 项 | 值 |
|---|---|
| 入口 index-*.js | 1294 KB（T5-4 前 1283 KB；评分工作台展示代码并入入口，未重新 lazy） |
| MarketResearch chunk | 516 KB |
| CSS | 70 KB（important 选择器策略成本，T5-1 起记录） |
| ProductScoringSection 运行时 chunk | 0（P0 契约保持） |

## 9. 测试

| 套件 | 结果 |
|---|---|
| `npm test`（含评分适配层回归，冻结分布锁） | ✅ **1/191/448/169/191 不变** |
| `test:golden` / `test:sync` / `test:web-persistence` | ✅ 76 / 零差异 / 通过 |
| `npm --prefix ozon-react run build` | ✅ |
| `verify:scoring-build`（含新增 T5-4 UI 契约：SearchInput / DecisionBadge / Drawer 消费 strengths·risks / 子组件禁 import scoringEngine·scoring_rules / 编排层必须经 scoreAllCandidates） | ✅ |

## 10. 评分对拍

`_audit/tmp/verify-ui-audit-identity.js`：index 3/77/213/555/888 五条 totalScore/grade/decision/context/marketScaleScore/candidateStrengthScore/六维/supplyGap/status **逐位一致**（UI 适配层 vs 审计 JSON）。

## 11. 未解决问题（如实登记）

1. **本环境无浏览器渲染/截图能力**：视觉验收（1920/1440 截图、Drawer 交互、字号观感）需需求方在生产部署后人工确认；本轮不声称已完成视觉验收。
2. WP 页面标题压缩选择器是防御性列表（.entry-title/.page-title/article h1 等）；若部署后主题渲染的标题容器不在列表内，需按截图补一个选择器。
3. 旧业务页面（非评分页）残留的 9-11px 字号与小卡片风格未在本轮统一（属 T5-5 polish 范围，任务书明确本轮不碰）。
4. `morandi-*` 类名保留（remap 实现），彻底清理类名属后续技术债，不在本轮。

## 12. 回滚方式

本轮未 merge main。如需回滚：弃用 `feat/v3-t5-visual-system`（或 revert 其 merge）；生产已知稳定基线仍为 main `8a04e1b`（P0 修复版）。

## 13. 本轮停止 Gate

T5-3 + T5-4 完成，**停止**。未进入 T5-5、未 merge main。等待人工线上视觉验收。
