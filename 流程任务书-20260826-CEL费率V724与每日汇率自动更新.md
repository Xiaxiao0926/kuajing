# 流程任务书 — CEL 资费表 V7.24 更新 & 每日汇率自动更新（2026-08-26）

> 交接对象：后续接手本项目的 AI Agent 或工程师
> 任务编号：FYZSXNB-KUAJING-TARIFF-V724-FX-AUTO-001
> 状态：**已完成并上线**（本任务书为事后完整归档，无未完成代码工作）
> 仓库：`https://github.com/Xiaxiao0926/kuajing`（本地 `D:\ozon`，分支 main）
> 线上地址：`https://fyzsxnb.com/kuajing/`

---

## 0. 交接速览

| 项 | 状态 |
|---|---|
| HEAD / 远程 | `a0dfa64` = origin/main（已推送，同步） |
| 线上部署 | 已部署 `a0dfa64`（DEPLOYED_COMMIT 一致），线上 bundle `index-BMWZJd3i.js` 已验证含全部新功能 |
| 任务A 资费表 V7.24 | commit `288a374`，已上线 |
| 任务B 汇率每日自动更新 | commit `a0dfa64`，已上线 |
| 测试基线 | npm test 44/44 · golden 76/76 · sync 零差异 · web-persistence 通过 · vite build 成功 |
| 遗留待办 | 见 §5（服务器临时目录清理、CI FTP 修复，均不影响线上运行） |

两个任务的完整时间线：用户更新官方 CEL 运费表 → 全渠道费率更新上线（任务A）→ 用户发现页头仍显示旧版本号"V5.23"且汇率未格式化 → 修复硬编码 + 顺势实现每日汇率自动更新（任务B）。

---

## 1. 任务A：CEL 资费表 V5.23 → V7.24 全渠道费率更新

### 1.1 业务背景

用户在 `D:\ozon\运费计算` 目录提供了官方最新资费表《CEL产品资费表 V7.24.xlsx》（OZON-rFBS sheet），要求更新到网站。相比 V5.23：17 个 Ozon 渠道费率上调约 7-8%，HK 渠道不变。HK 规则沿用《T2-Gate0-CEL-HK核验报告.md》既有核验结论。

### 1.2 修改文件（commit `288a374`）

| 文件 | 变更 |
|---|---|
| `config/ozon_channels.json` | 唯一事实源。source 更新为 V7.24，source_date 2026-08-26，17 个渠道 `kg_rate_cny`/`fixed_fee_cny` 上调，HK 不变 |
| `tests/golden/ozon-cel-channels.json` | 运费期望值更新 + provenance.change_log 记录变更依据（黄金测试纪律） |
| `ozon-react/src/utils/t6/costScenario.test.mjs` | C16 利润断言 41.77→36.74（费率上调导致，测试意图不变） |
| `tests/scoring-golden/grade-boundaries.json` | A/B 边界相邻毛利率 49/48→51/50（物流成本升→总分降→边界移位，change_log 已记录） |

关键数值快照（change_log 存证）：
- `express_xs` 运费：5.46→5.90（0.05kg × 50.5元/kg[原46.8] + 3.37元/票[原3.12]）
- `standard_big` 运费：384.11→415.11（计费重13.333kg × 28.1元/kg[原26] + 40.44元/票[原37.44]）
- HK 两例（115.00 / 451.00）不变

### 1.3 验证与部署

- 测试：npm test / test:golden / test:sync / test:web-persistence 全绿 + vite build 成功
- 部署：手动部署（GitHub Actions FTP 当时故障，见 §5.2），线上验证通过

---

## 2. 任务B：每日汇率自动更新 + 页头动态版本号（commit `a0dfa64`）

### 2.1 需求来源

用户指出："这里没改啊（页头仍显示 V5.23），同时我想每天自动更新汇率"。根因：`OzonCalc.jsx:42` 的版本号是**硬编码字符串**，资费配置更新不会传导到 UI；汇率 `R` 未格式化（显示 17 位小数）。

### 2.2 架构设计

**数据源三级降级（RUB per CNY，即 1¥ = X₽）：**

| 优先级 | 数据源 | 说明 |
|---|---|---|
| 主 | `https://www.cbr-xml-daily.ru/daily_json.js` | 俄罗斯央行官方每日牌价，CORS 开放免费。取 `Valute.CNY.Value / Nominal` |
| 备 | `https://open.er-api.com/v6/latest/CNY` | 市场汇率，取 `rates.RUB` |
| 兜底 | `config/settings.json` 的 `rub_per_cny`（当前 13） | 构建时固化值，双源失败时保持，不写缓存（下次访问自动重试） |

**核心机制：**

1. **日缓存**：localStorage 键 `fx-rate-daily-v1`，按业务时区（Asia/Shanghai）的自然日缓存，当日命中则不发网络请求。挂载时刷新一次（`App.jsx` → `refreshDailyRate()`）。
2. **防护**：合理性区间 8–20 ₽/¥ 越界拒绝；数据源时间戳距今 >10 天视为陈旧拒绝（防 CDN 缓存污染）；单请求 8s 超时。
3. **live binding 生效链**（关键设计，见 §6.3）：`ozonEngine.js` 的 `rubPerCny`/`R` 由 const 改为 `let` 导出 + `setLiveRubPerCny()` 运行时写入。引擎所有换算函数的**默认参数在调用时取值**，11 个组件的既有 `import { R, rubPerCny }` 引用无需任何改动，汇率更新全局即时生效（依赖 ESM live binding，Rollup 打包后仍保真——已验证，见 §4.3）。
4. **React 集成**：`useSyncExternalStore` hook（React 18.2），汇率变化触发使用方重渲染。
5. **WB 面板同步**：`WBCalc.jsx` 订阅汇率并同步写入 `settings.rubPerCny`（WB 引擎经 settings 消费汇率，与 Ozon 的 live binding 是两条消费路径，故需分别接）。
6. **透明标注**：页头显示如 `汇率 1¥ = 12.4378₽（俄罗斯央行 2026-08-26自动更新）`；兜底时显示 `（配置值）`。

### 2.3 文件清单

**新增（3）：**

| 文件 | 职责 |
|---|---|
| `ozon-react/src/utils/exchangeRate.js` | 每日汇率服务：三级数据源、解析与防护、日缓存、订阅通知、`refreshDailyRate()` 入口 |
| `ozon-react/src/utils/useExchangeRate.js` | React hook（useSyncExternalStore 封装） |
| `ozon-react/src/utils/exchangeRate.test.mjs` | 27 项单测：CBR/ER-API 解析（Nominal 换算/新鲜度/异常拒绝）、缓存命中、主源失败落备源、双源失败落兜底、live 写入。已接入 npm test 链 |

**修改（6）：**

| 文件 | 变更 |
|---|---|
| `ozon-react/src/utils/ozonEngine.js` | ① `rubPerCny`/`R` 改 `let` live binding + 新增 `setLiveRubPerCny()`（非法值拒绝且不变现状）② 新增 `TARIFF_VERSION`（正则从 `channelsData.source` 提取版本号，如 V7.24）③ 头注释更新 |
| `ozon-react/src/components/OzonCalc.jsx` | 页头：硬编码 "V5.23" → `TARIFF_VERSION`；汇率 `{R}`（17位小数）→ `formatRubPerCny(rateInfo.rubPerCny)` + 数据源/日期标注；订阅 `useExchangeRate()` |
| `ozon-react/src/components/ShippingCalc.jsx` | 硬编码 "V5.23" → `TARIFF_VERSION`（遗留组件，不在路由，但避免误导） |
| `ozon-react/src/components/WBCalc.jsx` | 汇率统一接 live：初始化取 `getRateInfo()`，effect 同步 `rateInfo` 变化写入 settings 并持久化；页头显示来源标注。**原"强制迁移为 DEFAULT_SETTINGS.rubPerCny"逻辑删除**（被 live 汇率取代） |
| `ozon-react/src/App.jsx` | `DashboardApp` 挂载时 `refreshDailyRate()` + `useExchangeRate()` 订阅触发整树重渲染 |
| `package.json` | test 脚本链尾追加 `node ozon-react/src/utils/exchangeRate.test.mjs` |

### 2.4 用户决策记录（重要）

- **2026-08-26**：用户确认暂不加"手动锁定汇率"开关，按央行牌价自动跑。若日后实际结汇价与央行牌价偏差大影响决策，再考虑加锁（设计上 `refreshDailyRate` 已支持，改动点仅在 UI 开关 + 跳过自动 apply）。
- 配置兜底值 13 与当日实际牌价（约 12.44）有差异——上线后测算用央行牌价，属预期行为。

---

## 3. 完整执行记录（时间线）

| 时间 | 动作 | 结果 |
|---|---|---|
| 08-26 上午 | 恢复前 Agent 现场（review-fix-002 收尾）→ commit `f6970ba` 部署 | 上线 |
| 08-26 上午 | GitHub Actions Run #20 FTP 部署失败（连接超时）→ 用户去页面重跑 | 结果未知（遗留，见 §5.2） |
| 08-26 中午 | 用户更新官方 V7.24 表 → 任务A 全流程（配置/黄金/测试/部署） | commit `288a374` 上线 |
| 08-26 下午 | 用户反馈页头未变 + 提出每日汇率需求 → 本任务书 §1/§2 全部工作 | commit `a0dfa64` 推送 GitHub 成功 |
| 08-26 下午 | 手动部署 + 线上验证（见 §4.3） | 15 文件上传成功，线上新 bundle 生效 |
| 08-26 晚 | 编写本任务书 + CHANGELOG 登记 + .gitignore 修复 | docs commit |

**测试证据（可复跑）：**

```
cd D:\ozon
npm test                    # 44/44 通过（含新增 exchangeRate 27 项）
npm run test:golden         # 76/76 通过
npm run test:sync           # 零差异
npm run test:web-persistence # 通过
cd ozon-react && npm run build  # 成功
```

---

## 4. 标准部署流程（手动，当前 CI 故障期间的唯一通道）

### 4.1 打包

```powershell
cd D:\ozon\ozon-react; npm run build   # vite base 默认 './'，与 CI 的 KUAJING_BASE_PATH=./ 一致
cd D:\ozon
if (Test-Path deploy) { Remove-Item -Recurse -Force deploy }
New-Item -ItemType Directory -Force deploy\kuajing-persistence | Out-Null
Copy-Item wordpress\kuajing-persistence\kuajing-persistence.php deploy\kuajing-persistence\
Copy-Item wordpress\kuajing-persistence\README.md deploy\kuajing-persistence\
Copy-Item -Recurse ozon-react\dist deploy\kuajing-persistence\dist
if (Test-Path deploy\kuajing-persistence\dist\data) { Remove-Item -Recurse -Force deploy\kuajing-persistence\dist\data }
git rev-parse HEAD | Out-File -Encoding ascii deploy\kuajing-persistence\DEPLOYED_COMMIT
```

> `dist/data` 必须删除：服务器数据走插件 PHP API，bundle 内数据目录会覆盖线上真实数据。

### 4.2 部署（Hostinger MCP）

```
工具: hosting_deployWordpressPlugin
参数: { domain: "fyzsxnb.com", slug: "kuajing-persistence",
        pluginPath: "D:\\ozon\\deploy\\kuajing-persistence" }
```

注意：工具返回的 uploadDirName 会带随机后缀（如 `kuajing-persistence-vGmT4tRq`），但**以线上页面实际引用为准**——插件目录 `wp-content/plugins/kuajing-persistence/` 会被正确更新（已多次验证）。上传后残留的随机后缀目录需在 hPanel 清理（§5.1）。

### 4.3 线上验证（必做，本次实际执行过的验证）

```powershell
# 1. 页面引用的 bundle 是否为最新 hash
$r = Invoke-WebRequest -Uri "https://fyzsxnb.com/kuajing/" -UseBasicParsing
($r.Content | Select-String -Pattern 'kuajing-persistence/dist/[^" ]+' -AllMatches).Matches.Value | Select-Object -Unique

# 2. bundle 内容抽查（本次验证项：cbr-xml-daily.ru / open.er-api.com / fx-rate-daily-v1 / V7.24 / 自动更新文案 均命中；旧 V5.23 字符串已不存在）
$r2 = Invoke-WebRequest -Uri "https://fyzsxnb.com/wp-content/plugins/kuajing-persistence/dist/assets/<新hash>.js" -UseBasicParsing
# PowerShell 中文匹配需 UTF-8 解码：[System.Text.Encoding]::UTF8.GetString($r2.RawContentStream.ToArray())
```

补充：汇率功能为浏览器端行为（fetch 外部 API + localStorage），服务器侧验证仅能确认代码已上线；真实数据源连通性以浏览器打开页面看页头标注为准（显示"俄罗斯央行 …自动更新"即主源成功）。

### 4.4 live binding 打包保真验证方法（本次执行，供日后回归）

Rollup 理论上会保留 ESM live binding，但**曾验证过才可信**：在 dist/assets/index-*.js 中确认——引擎模块汇率变量为 `let` 声明、setter 函数重赋值同一变量、换算函数默认参数引用同一变量（本次该变量被 139 处共享引用）。若未来升级 Vite/Rollup 后汇率自动更新失效（页面显示配置值 13），优先怀疑此处。

---

## 5. 遗留待办（按优先级）

### 5.1 hPanel 清理服务器临时目录（低风险，建议尽快）

`wp-content/plugins/` 下有 4 个可删的历史/临时目录（每次 MCP 部署会残留随机后缀目录，如本次的 `kuajing-persistence-vGmT4tRq`，另有 3 个更早的）。**删除前逐一确认不是 `kuajing-persistence` 正式目录**。约释放数十 MB。

### 5.2 GitHub Actions FTP 部署失败（需要修复或换通道）

Run #20 失败于 lftp 连接超时，与代码无关（更可能是 Hostinger 对 CI IP 的连接限制）。两条路：
- 查 Actions 日志中 lftp 报错：超时=IP 层问题，530=凭证问题；
- 或弃用 FTP，改用 Hostinger API 部署（本次手动流程已验证可行，可脚本化为 CI job）。
在修复前，**所有部署走 §4 手动流程**。

### 5.3 观察项

- 每日汇率首日运行后，确认页头正常显示"俄罗斯央行 …自动更新"（若长期显示"配置值"，按 §4.4 排查）。
- WB 面板汇率展示与 Ozon 面板是否一致（两面板消费路径不同，理论已对齐）。

---

## 6. 接手人注意事项（禁止事项与关键约束）

### 6.1 配置唯一事实源纪律（沿袭既有约定）

- 渠道费率**只改** `config/ozon_channels.json`，改后跑 `npm run sync`（生成 `src/generated/`）——引擎与 UI 数值禁止手改。
- 汇率兜底值只改 `config/settings.json` 的 `rub_per_cny`。

### 6.2 黄金测试纪律

费率/边界变更导致 golden 期望值更新时，**必须**在 fixture 的 `provenance.change_log` 记录日期、原因、新旧值与换算依据（本次任务A的 3 个 fixture 均已示范）。

### 6.3 live binding 依赖（本任务新增的隐性约束）

- `ozonEngine.js` 的 `rubPerCny`/`R` **必须是 `let` 导出**，`setLiveRubPerCny()` 是唯一合法写入口。改回 const 会让每日汇率静默失效（表现为页面永远显示构建时配置值）。
- 组件中**不要**把 `R`/`rubPerCny` 的值在模块顶层拷贝到局部 const（会冻结快照）；需要当前值时在渲染/计算时引用。
- 汇率防护区间 8–20 ₽/¥ 与 10 天新鲜度是数据源污染防线，放宽前先想清楚。

### 6.4 版本号显示

资费表版本号一律用 `TARIFF_VERSION`（从配置 source 提取），**任何 UI 不得再硬编码版本字符串**——本次任务B的起因就是硬编码 V5.23 未随配置更新。

### 6.5 工作区无关文件（勿动、勿提交）

`bydCP0818.sh`、`fybydcp0818.html`、`hwj07_audit/`、`reports/`、`tools/` 为用户其他工作（BYD 相关），与本仓库无关；`ozon-react/public/data/manifest.json` 的改动是运行时时间戳噪音。均不要纳入提交。`deploy/` 为临时打包目录（已加入 .gitignore），可随时删除重建。

---

## 7. 相关文档索引

| 文档 | 内容 |
|---|---|
| `CHANGELOG.md` | 2026-08-26 两条变更登记（任务A/B） |
| `T2-Gate0-CEL-HK核验报告.md` | HK 渠道规则核验结论（V7.24 沿用） |
| `FYZSXNB-KUAJING-CALC-ENGINE-GLM-REVIEW-FIX-002-REPORT.md` | 前一任务（引擎核验修复，commit f6970ba） |
| `RUNBOOK.md` | 启动/测试/运维手册 |
| `AGENTS.md` / `BUSINESS_RULES.md` | 行为约束与业务公式 |
