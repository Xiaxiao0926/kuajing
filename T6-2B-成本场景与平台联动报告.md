# T6-2B-成本场景与平台联动报告

> 日期：2026-08-14 ｜ 范围：T6-2A hardening → T6-2B1（Ozon 成本场景）→ T6-2B2（WB 成本场景 + 跨平台比较）
> 合并：main `f1ef799` → `0b78bac` → `797b6c2`（全部 --no-ff）

---

## 1. 目标与范围

把「核算面板 ↔ SKU 项目」打通为**不可变成本场景**：从候选真实数据预填核算面板 → 用户在面板填写真实成本假设 → 一键冻结为只读快照挂到项目 → 基线场景驱动 Stage Gate 的 COSTING 段 → Ozon/WB 双平台并列比较。

**不在范围**：任何引擎公式改动、任何 config 费率改动、评分分布改动、T7 及之后阶段。

---

## 2. Phase A：T6-2A 加固（`fix(t6-2a)` `2b2758d`，merge `f1ef799`）

- **`stageModel.js`**（新）：阶段唯一事实源（PIPELINE..REVIEW 9 段，`SAMPLING_INDEX=3`）；Store/Gate/UI 统一引用，禁止各自维护阶段列表。
- **`gateEngine.js`**（重构）：路径 Gate——`direction` SAME/FORWARD/BACKWARD；FORWARD 时 path=(from,target] 逐段聚合，check 带 `stage` 归属；非法 stage **fail-close throw**（绝不把未知阶段当空 checks→GREEN）；BLOCKED_LOGISTICS 硬阻断精确语义（当前<SAMPLING 且目标≥SAMPLING 才触发一次）；verdict 优先级 RED>YELLOW>NOT_EVALUATED>GREEN。
- **`stageTransition.js`**（新）：流转 domain action——SAME throw；BACKWARD 跳过前向 Gate（stage_change 默认理由「阶段回退」，无 gate_override）；FORWARD 的 YELLOW/RED **必填理由**，为空 throw 且 stage 不变、不产生任何日志；有理由则写 `gate_override` + `stage_change`。
- **`t6Store.js`**：`setProjectStage` 用 stageModel 校验；`setWorkflowNode` 审计——状态变化写 1 条 `workflow_change`（from→to，reason=节点标题），仅备注变化不写日志，note=undefined 保留旧值、可清空为 ''。
- **UI**：ProjectDetailPage 路径 Gate 面板（按 stage 分组检查、BACKWARD 回退提示、YELLOW/RED 理由输入）；Workflow 备注本地草稿 onBlur 保存；ProjectListPage 移除列表内生命周期按钮与 window.prompt（动作收敛到详情页）。
- **测试**：gateEngine 33（新增 I18 非法目标/I19 非法当前/I20 SAME/I21 BACKWARD/I22 RED 必填理由）；t6Store 56（新增 I17 workflow 审计）。

## 3. Phase B1：Ozon 成本场景（`feat(t6-2b1)` `ce4949c`，merge `0b78bac`）

- **CostScenario 实体**（t6Store）：`t6.costScenario.<uuid>` 逐实体持久化，**仅 create/read**（无 update/delete API）；payload 禁带系统字段（id/createdAt/schemaVersion 注入 throw）；项目存在性 + 快照归属校验（sourceSnapshotId 必须 = 立项快照，fail-close）；`project.costing.scenarios` 追加 + **首个自动基线**；日志 `cost_scenario_create` / `cost_baseline_change`；`setProjectBaselineScenario` 跨项目 throw、引用型变更（场景字节不变）。
- **`costScenarioAdapter.js`**（新）：
  - `buildOzonPrefill`——只取 `price_rub`（实际售价，**绝不 ×0.6**）/`weight_kg`/`dims`/`commission_rfbs`（**绝不回退 fbs/fbo/fbp**）；采购/国内运费/贴标/广告/支付/代理/退货 7 项**绝不虚构**（留空由用户填写）。
  - `buildOzonResolvedConfig`——从 `config/ozon_channels.json`（唯一事实源）按 id 定位**原始渠道记录**整体冻结 + `source/source_date/verified_by` meta；汇率双语义并存：`rubToCny=ozon_rub_to_cny(0.09)`（引擎实际使用）与 `celRubPerCny=rub_per_cny(12)`（CEL 资费上下文存档），**禁止统一**。
  - `buildOzonScenarioPayload`——inputPayload=面板全部真实输入；outputPayload=`calcChannelProfit` 输出**原文**；`calculatorVersion='ozon-rfbs-single-v1'`。
  - `scenarioSummary`——统一摘要（id/platform/channel/priceRub/profitCny/profitMarginPct/calculatorVersion），为 B2 跨平台比较预留。
- **OzonCalc projectContext 模式**：显式状态传入（不读残余 activeProjectId）；prefill 仅挂载一次（key 按 projectId 强制重挂）；项目模式下编辑**不写共享持久化键**；[保存此方案到项目] 冻结当前方案。
- **Gate 真实化**：COSTING 段 `cost_scenario`/`target_price` 从 `deps.scenarios` 读真实场景——无场景 FAIL(RED)、基线毛利 ≥15 PASS、<15 WARN(YELLOW)、未设基线 FAIL；supplier 等未实现域仍 NOT_EVALUATED。
- **测试**：costScenario 44（C1 创建/自动基线、C2 二次场景不改基线、C3 系统字段注入、C4 append-only、C5 跨项目/引用型、C6 prefill 白名单、C7 resolvedConfig 冻结+verbatim、C8 摘要、C9 创建校验）。
- **修复**：`createCostScenario(args)` 改为整参校验——原实现先解构再校验导致注入字段被丢弃（测试 C3 抓出）。

## 4. Phase B2：WB 成本场景 + 跨平台比较（`feat(t6-2b2)` `8271d3f`，merge `797b6c2`）

- **`buildWbPrefill`**——只预填 5 项：productName、`actualWeightG=weight_kg×1000`、尺寸、`sellerRevenueRub←price_rub`（参考售价）。**佣金绝不预填**（即使 `commission_rfbs=99`，W9 断言）；WB 佣金/线路/成本假设全部用户填写（WB 是独立利润模型，禁止移植 Ozon 佣金）。
- **`buildWbResolvedConfig`**——冻结**完整费率版本快照**（tariffId/routeId/routeName/effectiveFrom/effectiveTo/weightRoundingG/尺寸重量限制/tiers/反向规则/sourceName），**不是只存 routeId**；设置子集（rubPerCny/阈值）；`calculatorVersion='wb-order-v2'`（**公式不变**，仅元数据）。
- **`buildWbScenarioPayload`**——inputPayload=单订单核算全部输入（含反向配送字段）；outputPayload=wbEngine 输出**原文**（logisticsCalc/profitCalc/reverseCalcResult/breakEvenPriceRub）。
- **UI**：WBCalc/CalculatorTab projectContext 模式（预填一次、[保存此方案到项目]、售价标注「来自候选市场的参考售价（可修改）」）；ProjectDetailPage 成本 Tab 增加 [使用 WB 核算] + 跨平台方案比较表（平台/场景/线路/售价₽/毛利率/冻结时间/基线），注明「比较值来自各平台现有核算引擎；不跨平台重新计算费用」；WB 场景标注「仅测算参考」，基线仅人工切换。
- **Gate 平台统一**：`scenarioMarginPct` 统一毛利口径（OZON=profitRate；WB=profitCalc.profitMargin）；`target_price` 统一售价口径（price/sellerRevenueRub）。
- **测试**：wbScenario 42（W1 预填白名单、W2 费率版本完整快照、W3 版本元数据、W4 引擎输出 verbatim、W5 store 创建、W6 摘要、W7 OZON/WB 共存统一口径、W8 Gate WB 基线判定、W9 佣金不移植、W10 基线 human-only）。

---

## 5. 不变式（全部回归验证）

| 不变式 | 结果 |
|---|---|
| wbEngine / ozonEngine / wb_calc.py 公式 | **0 改动**（git diff 空） |
| config/*.json 费率 | **0 改动**（仅生成物重跑） |
| 评分分布 A1/B191/C448/D169/不可评级191 | **不变**（scoringDataAdapter 13 断言锁定） |
| 汇率双语义 rub_per_cny=12 / ozon_rub_to_cny=0.09 | **并存不统一**（C9/W2 断言） |
| Ozon SingleTab 实际售价（无 ×0.6）；MultiTab ×0.6 | 原样保留（TD-14 未触碰） |
| 黄金案例 76 / 双端对拍零差异 | 全绿 |
| T6 四套测试 | 56+42+44+42 = 184 断言全绿 |

## 6. 全量回归（main 上）

```
npm test              ✅ wbEngine 65 + scoring 81/20/13/22 + T6 56/42/44/42 + scoring-golden 56 + python 31
npm run test:golden   ✅ 76/76
npm run test:sync     ✅ 零差异（16 边界 + 2 版本）
npm run verify:scoring-build ✅
npm run test:web-persistence ✅
vite build            ✅
```

## 7. 已知限制（如实登记）

- 跨平台比较表只并列展示各平台引擎输出，**不跨平台重算费用**（需求方明确口径）。
- TD-23 新登记：WB 反向配送「实际值字段」保存时不校验与估算值一致性（场景冻结后不可修正，只能新建场景）。
- TD-22 保持：RU-YYYY-NNN 并发编号依赖未来 WP 服务端原子序列。
