# D:\ozon 项目整改任务书 V3（AI 模型无关的可维护工程）

> 版本：V3.1（取代 V2；已纳入 2026-08-14 评审 4 项修订）
> 目标升级：从「整理 GLM 生成的项目」升级为「把项目改造成 **AI 模型无关的可维护工程**」——业务规则、代码结构、测试、数据路径、禁止事项全部外显化到文档与配置层。
> 状态：T0 执行中

---

## 0. V2 → V3 变更记录

| # | 变更 | 来源 |
|---|---|---|
| 1 | 按重读结果修正状态：P0-3 快照、P0-6 push **已完成**；P1 大部分完成但有遗留与回归 | 重读 |
| 2 | 新增 **文档层**：`AGENTS.md`、`ARCHITECTURE.md`、`BUSINESS_RULES.md`、`RUNBOOK.md`、`CHANGELOG.md`、`TECH_DEBT.md` | 评审 |
| 3 | 新增 **黄金业务案例** `tests/golden/*.json`（人工核算确认的订单输入/期望输出） | 评审 |
| 4 | 新增 **AI 范围限制规则**（禁止顺手重构/升级/删数据，技术债只记录到 TECH_DEBT.md） | 评审 |
| 5 | 新增 **Git 分支纪律**（一任务=一分支=一 commit） | 评审 |
| 6 | 新增 **环境变量约定**（OZON_DATA_DIR / WB_DATA_DIR / CONFIG_DIR，禁止硬编码本机路径） | 评审 |
| 7 | 新增 **模型路由约定**（Pro 做技术负责人 / Flash 做日常开发，写入 AGENTS.md） | 评审 |
| 8 | 修复重读发现的回归：`npm test` 退出码 1、`tunnel.js` 依赖缺失、Python 端路径仍硬编码、`cc-switch` 未删、`public/` 孤儿脚本未归档 | 重读 |
| 9 | V3.1 修订：审计报告入库（仅 raw/tmp 忽略）、平台无关测试命令、Lead/Fast 去型号化、tunnel 可选依赖、T2 迁移顺序冻结、golden provenance、U1-U4 裁决 | 评审 |

---

## 1. 当前实际状态盘点（2026-08-14 重读结果）

### ✅ 已完成（勿重复执行）

| 项 | 证据 |
|---|---|
| P0-3 本地快照 | commit `7456a6d`（全量快照 + 测试基线 65/31） |
| P0-6 push GitHub | reflog：`update by push` ×2；`origin/main = HEAD = d6a197d` |
| P1-1 归档 29 个脚本 | commit `d6a197d`，根目录 43→14 文件，`_archive/{debug,analysis,deprecated,reports,migration}` |
| P1-3 根 README.md | 已存在（150 行：4 应用/启动/数据流/目录结构/路径配置） |
| P1-4 Node 端路径三层优先级 | `config.js` 已实现 `BASE_PATH → data/ → Legacy` + `LEGACY_PATH_ENABLED` |
| P1-5 npm test 接入 | 根 `package.json` 已有 `test` 脚本 |
| Secret 结论 | 重扫干净：无 `.env/pem/key/ai_config`；tracked 文件零高危模式命中；`jd-union-service` 密钥走环境变量；init commit 历史扫描无真实密钥 |

> 注：P0-0/P0-4 的**审计报告从未落盘**（`_audit/` 不存在），本次重读已补扫且结论干净，V3 任务 T0-1 只补写报告，无需再扫。

### ⚠️ 重读发现的遗留与回归（V3 新增任务）

| # | 问题 | 严重度 |
|---|---|---|
| R1 | **`npm test` 退出码 1**：React 部分 65/65 通过，但脚本中 `python` 在本机解析到 WindowsApps 空壳（`py` 可用）。测试脚本跨环境脆弱 | 高 |
| R2 | **`tunnel.js` 回归**：P1 改动后 `require('localtunnel')`，但 localtunnel 不在 `package.json`/`node_modules`（只有 ngrok），`node tunnel.js` 直接报错退出。原始版本引用 `D:/tunnel/...` 同样失效 | 高 |
| R3 | Python 端路径仍硬编码：`ozon-product-analyzer/app.py:25 DATA_DIR = r"d:\ozon\选品"`；`wb_panel.py:29 COMMISSION_FILE = r"d:\ozon\运费计算\wb佣金.xlsx"` | 中 |
| R4 | `cc-switch/`（仅含 `.git` 的残留嵌套仓库）未删除 | 低 |
| R5 | `public/` 有 29 个孤儿脚本（`test*.js`、`mapblock.js`、`rr_func.js`、`tblock.js`、`subset.js`、`served_script.js`、`check.js`）：`index.html` 为内联脚本、无任何引用，属遗留调试文件 | 低 |
| R6 | `ozon-react/prd.md`/`technical.md` 是旧吹风机项目的过期文档，与实际架构不符 | 低 |
| R7 | `uploads/` 空目录、`cloudflared.exe`（51MB，gitignored 但占磁盘） | 低 |

---

## 2. 目标（V3 完整版）

| 序号 | 目标 | 衡量标准 |
|---|---|---|
| G1 | 代码安全 | 全部源码在 GitHub；secret 审计报告落盘；无敏感信息入历史 |
| G2 | 结构清晰 | 根目录只剩应用/文档/配置/归档，无孤儿文件 |
| G3 | **规则单一化 + 外显化** | 费率/佣金/公式只存在于 `config/*.json` 与 `BUSINESS_RULES.md`，双端同源且对拍一致 |
| G4 | **AI 可接管** | 任意新 AI（DeepSeek/GPT/GLM）读 `AGENTS.md` 后 10 分钟内可安全启动、测试、小改，且不会越界 |
| G5 | 可回归 | `npm test`（React 65 + Python 31）与黄金案例一键全绿，改费率不回归 |
| G6 | 无机器绑定 | 代码中零硬编码本机绝对路径；一切经环境变量 + 文档化默认值 |

---

## 3. 执行任务清单

### T0 — 收尾（半天）

| # | 任务 | 命令/动作 | 验收 |
|---|---|---|---|
| T0-1 | 补写 Secret 审计报告 | 把重扫结论写入 `_audit/secret_audit_report.md`（含扫描模式、范围、结论、`jd-union-service` 环境变量说明）。**报告纳入 Git 跟踪**（审计历史是仓库文档的一部分）；仅 `_audit/raw/`、`_audit/tmp/` 加入 `.gitignore` | 报告被 `git ls-files` 跟踪；`git status` 干净 |
| T0-2 | 修复 R1 `npm test` | 建立平台无关命令：`npm test` / `npm run test:python` / `npm run test:golden` / `npm run test:sync`；Python 经 `scripts/run-wb-py-test.js` 探测启动器（Windows `py -3` → `python3` → fallback `python`），React 部分不变 | 本机 `npm test` 退出码 0，输出 65+31 全过 |
| T0-3 | 修复 R2 tunnel.js | **按可选工具处理**：检测不到 `localtunnel` 时打印"Tunnel feature unavailable. Install optional dependency: npm install localtunnel"并**正常退出（exit 0）**，不新增正式依赖；若确认日常必须再升级为 dependency | `node tunnel.js` 缺依赖时不崩溃、给出明确指引；有依赖时打印公网 URL |
| T0-4 | 删除 R4 `cc-switch/` | 先确认无独立未推送历史（已确认与主仓库同源）后 `Remove-Item -Recurse -Force` | `cc-switch` 不存在 |
| T0-5 | 处理 R5 孤儿脚本 | `public/test*.js`、`mapblock.js`、`rr_func.js`、`tblock.js`、`subset.js`、`served_script.js`、`check.js` 移入 `_archive/deprecated/`（`git mv` 保留历史）；`index.html`/`cleaned-data.html`/`served.html` 保留 | `public/` 仅剩 3 个 html；`npm start` 后 `/` 页面正常 |
| T0-6 | 处理 R6/R7 | `prd.md`/`technical.md` 移入 `ozon_hair_dryer_analysis/`（它们属于该项目）；删空 `uploads/`；`cloudflared.exe`：先全仓 grep `cloudflared` 确认无启动脚本/README/bat/ps1 引用，确认后删除（已 gitignored，不影响仓库） | 过期文档不再误导新人；cloudflared 无引用残留 |

### T1 — 文档层（AI 接管的"项目宪法"，核心新增）

| # | 任务 | 内容要点 | 验收 |
|---|---|---|---|
| T1-1 | **`AGENTS.md`**（最高优先级） | 见 §4 规范：项目是什么、4 应用职责、生产/归档目录、禁删数据、禁改公式清单、改前/改后必跑测试、配置唯一事实源、禁写本机路径、禁提交密钥、禁无需求重构、模型路由约定、分支纪律 | 新 AI 读一遍即可回答"能改什么/不能改什么" |
| T1-2 | `ARCHITECTURE.md` | 4 应用拓扑、数据流（市场价→server.js→React；config→双引擎）、端口、关键文件索引 | 与代码一致 |
| T1-3 | `BUSINESS_RULES.md` | WB 运费公式（100g 取整、跳档、多包裹独立）、Ozon CEL 渠道规则、利润口径、反向赔偿倍数、佣金/汇率口径；**注明来源文档**（需求规格说明书 §4/§6/§7.6） | 公式与 `config/` 及两引擎一致 |
| T1-4 | `RUNBOOK.md` | 启动/停止 4 应用、换数据路径、更新费率步骤、发布流程、备份恢复、故障排查；**附当前 AI 角色→模型映射表**（Lead/Fast 分别可用的具体模型名，模型换代只改此表） | 按文档可完成日常运维 |
| T1-5 | `CHANGELOG.md` | 从 git 历史回填：init → 快照 → P1 → V3 各阶段；此后每次变更必记 | 与 git log 对齐 |
| T1-6 | `TECH_DEBT.md` | 登记：NewDashboard 459KB/WBCalc 90KB 巨型组件、双端引擎对拍缺失（待 P2）、旧吹风机项目、public 数据冗余（根目录与 public/data 重复 xlsx） | 技术债有归处，AI 不得擅自处理 |

### T2 — 配置唯一事实源 + 黄金护栏（架构核心）

| # | 任务 | 动作 | 验收 |
|---|---|---|---|
| T2-1 | 建立 `config/` 唯一事实源 | **迁移顺序（严格）**：现有行为冻结 → 复制当前配置到 `config/*.json`（**不整理字段、不改公式、不重构引擎**）→ React `wbConfig.js` 改适配器（camelCase 导出不变→下游零改动）→ Python 改读 JSON → 65+31 全过 → 黄金案例全过 → 双端对拍零差异。**任何数字变化视为失败**，不接受"新实现更合理" | 改 JSON 一处，双端数值同变；迁移前后计算结果逐位一致 |
| T2-2 | Python 端接入 | `wb_data.py` 的 `TARIFFS_FILE`/`SETTINGS_FILE` 指向 `config/`（经环境变量 `CONFIG_DIR` + 相对路径回退）；`wb_panel.py` 的 `COMMISSION_FILE`、`app.py` 的 `DATA_DIR` 一并改为 `OZON_DATA_DIR`/`WB_DATA_DIR` 环境变量（修复 R3） | 本机零硬编码路径；`py wb_test.py` 仍 31/31 |
| T2-3 | **黄金业务案例** | `tests/golden/` 放 10-20 个订单 JSON：`{provenance, input:{...}, expected:{logistics, commission, profit}}`。**provenance 必填**：种子案例 `{"source_type":"spec_derived","verified_by_human":false}`；未来人工核算真实订单 `{"source_type":"real_order","verified_by_human":true}`（最高等级护栏）。种子来源：需求说明书 §15.5 反向赔偿表（500g 六场景）+ 现有 65 测试的 500g 订单 | `npm run test:golden` 全绿；每案例含 provenance 字段 |
| T2-4 | 双端对拍脚本 | `scripts/verify_sync.js`：读 `config/` → 分别喂 React 引擎（Node 子进程）与 `wb_calc.py` → 对 1g/100g/101g/300g/301g/20kg/超重/多包裹 输出 diff | 对拍零差异 |

### T3 — 结构治理（延后项，单独立项，不混入 T0-T2 提交）

| # | 任务 | 说明 |
|---|---|---|
| T3-1 | 巨型组件拆分（NewDashboard/WBCalc/FragrancePricing） | 按职责拆：页面只编排，表格/筛选/弹窗/图表独立；**无行数硬指标**；登记到 TECH_DEBT.md 后单独 branch 执行 |
| T3-2 | Vite 代码分割 | React.lazy 按路由节点懒加载；验收注明设备/网络/冷启动口径 |
| T3-3 | 1201 行选品数据接入评分面板（原 P3） | 产出 A/B 类推荐表 |

---

## 4. AGENTS.md 规范（T1-1 的内容模板）

```markdown
# AGENTS.md — AI 协作宪法
任何 AI（DeepSeek/GPT/GLM/Codex）在本仓库工作前必须先读本文件。

## 项目是什么
Ozon/WB 俄罗斯跨境电商运营工作区 = 4 应用 + 业务数据资产（见 ARCHITECTURE.md）。

## 目录分类
- 生产代码：ozon-react/、ozon-product-analyzer/、server.js、config.js、
  market_data_processor.js、analyze_with_cleaned_data.js、config/
- 业务数据（禁止删除/改动内容）：市场分析/、选品/、运费计算/、
  根目录 xlsx/pdf、ozon-react/public/data/
- 历史归档（只读）：_archive/
- 文档（先读后改）：本文件、ARCHITECTURE.md、BUSINESS_RULES.md、
  RUNBOOK.md、CHANGELOG.md、TECH_DEBT.md

## 禁止事项（无例外）
1. 禁止修改 wbEngine.js / ozonEngine.js / wb_calc.py 中的计算公式，
   除非需求方明确要求且更新 BUSINESS_RULES.md + 黄金案例。
2. 禁止提交 API Key / Token / Cookie / .env / *.pem / 任何凭据。
3. 禁止写死本机路径（D:\、E:\、C:\Users\）。一律用
   OZON_DATA_DIR / WB_DATA_DIR / CONFIG_DIR / BASE_PATH 环境变量
   + 文档化默认值。
4. 禁止顺手重构、升级依赖、统一风格、删"看起来没用"的数据、
   迁移技术栈。发现技术债只登记 TECH_DEBT.md。
5. 禁止无需求大规模重写（本项目最初由 GLM 生成，但历史来源
   不是重写理由）。
6. 禁止把业务数据 xlsx 推送公开仓库前不确认脱敏。

## 修改前必须读
- 涉及计算：BUSINESS_RULES.md + 对应引擎测试文件
- 涉及路径/配置：RUNBOOK.md §路径配置
- 涉及 UI：对应组件 + ARCHITECTURE.md §关键文件索引

## 修改后必须跑（真执行，不许口头声称"应该没影响"）
npm test                       # React 65 + Python 31
npm run test:python            # Python 端独立验证（跨平台，内部自动探测启动器）
npm run test:golden            # 黄金案例护栏
npm run test:sync              # 双端对拍
# 只允许使用以上平台无关命令，禁止直接调用 python/python3/py 启动器。

## 修改范围铁律
只改完成当前任务必须的文件。任何超范围改动必须先在回复中
说明理由并等待确认。

## Git 纪律
一个任务 = 一个分支（fix/ feat/ refactor/ 前缀）= 一个清晰 commit。
禁止连续多天工作后一次性提交。改坏了 git reset 即回滚。

## AI 角色（与模型型号无关；当前模型映射见 RUNBOOK.md）
- Lead（技术负责人）：架构决策、未知 Bug 分析、业务公式变更、
  跨端修改、数据迁移、大型重构、Git 事故处理。
- Fast（日常开发）：UI 小改、字段/文案、明确小 Bug、写测试、
  补文档、已批准方案的机械执行。
- Fast 不得自行决定是否重构系统；发现技术债只登记 TECH_DEBT.md。

## 任务工作流（复杂任务强制）
第一阶段只分析：列出涉及文件、数据流、拟改范围、风险，不写代码；
第二阶段小范围修改；第三阶段跑测试+黄金案例；
第四阶段 git diff 自查是否超范围；第五阶段提交。
```

---

## 5. 验收清单（V3 全量）

- [ ] `npm test` 退出码 0（React 65 + Python 31），跨 shell 可复现
- [ ] `tunnel.js` 可运行；`cc-switch` 已删；`public/` 无孤儿脚本
- [ ] Python 端零硬编码路径（app.py / wb_panel.py / wb_data.py）
- [ ] `config/*.json` + schema 存在，双端对拍零差异
- [ ] `tests/golden/` ≥10 案例，`npm run test:golden` 全绿
- [ ] 文档六件套（AGENTS/ARCHITECTURE/BUSINESS_RULES/RUNBOOK/CHANGELOG/TECH_DEBT）齐备且与代码一致
- [ ] `_audit/secret_audit_report.md` 落盘
- [ ] GitHub 与本地一致，历史无敏感信息

---

## 6. 未决项（已裁决，2026-08-14）

| 编号 | 裁决结论 |
|---|---|
| U1 | ✅ `_archive/` **保留在 GitHub**（只读历史归档对 AI 接管有价值；`git rm --cached` 无法抹除历史且无必要） |
| U2 | ✅ 先用**推导案例做种子**（`spec_derived` / `verified_by_human:false`），后续逐步补人工确认真实订单并升级为最高等级护栏 |
| U3 | ✅ 全仓 grep `cloudflared` 确认无引用后**删除** `cloudflared.exe` |
| U4 | ✅ 批准从 T0 开始执行（含本次 4 项修订） |

### 本次评审 4 项修订（已同步进本任务书）

1. `_audit/secret_audit_report.md` **纳入 Git 跟踪**（审计历史是仓库文档），仅 `_audit/raw/`、`_audit/tmp/` 忽略；
2. AGENTS 只出现**平台无关命令**（`npm test` / `npm run test:python|golden|sync`），Python 启动器探测封装进 `scripts/run-wb-py-test.js`；
3. AGENTS 用 **Lead/Fast 角色**替代具体模型型号（去模型绑定），当前模型映射放 RUNBOOK.md；
4. `tunnel.js` 按**可选工具**处理：缺 localtunnel 时提示并正常退出，不新增正式依赖。
