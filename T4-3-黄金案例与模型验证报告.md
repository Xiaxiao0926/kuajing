# T4-3 黄金案例与模型行为验证报告

> 阶段：T4-3（三层验证：Golden Cases / 单调性·不变式 / 维度验证矩阵）
> 状态：四组结果全部产出；**未改六维权重、未为 leader enrichment 调模型、未加 BSR 标签评分、未接 UI**。
> 结论待需求方验收；demand 维度两行 REVERSED 的根因分析见 §5，属口径语义问题而非代码 bug。

---

## 1. 交付边界（与任务书一致）

| 新增/修改 | 说明 |
|---|---|
| `tests/scoring-golden/*.json` | 10 个纯数据固件（紧凑确定性候选池 + 市场基准 + 期望断言），任何人可读、可执行 |
| `scripts/run-scoring-golden.js` | 固件跑器：逐 case 跑 `scoreProduct`/`buildExplanations` 并断言 |
| `ozon-react/src/utils/scoring/monotonicity.test.mjs` | 7 组单调性/不变式（以 golden 固件为基座，只改单一输入） |
| `scripts/t4-score-audit.js` | ① `mappedResults` 改用 `grade !== null`（NEEDS_DATA 诊断分不再混入 Top20%）；② 维度验证矩阵 |
| `package.json` | `test:scoring` 并入单调性；新增 `test:scoring-golden`；`npm test` 全链覆盖四层 |
| `T4-3-黄金案例与模型验证报告.md` | 本报告 |

黄金固件生成器位于 `_audit/tmp/`（gitignore，不入库）；固件本身是生成后冻结的 JSON。

---

## 2. 第一层：Golden Cases（10 固件 / 56 断言，全绿）

| 固件 | 关键断言 |
|---|---|
| `excellent-mapped.json` | grade=A、totalScore≥80、context=HIGH、ELIGIBLE/SAMPLE_VALIDATION、无 NEEDS_DATA、coverage=1、解释含"同类市场" |
| `poor-negative-margin.json` | grade∈{C,D}、totalScore<65、MARGIN_RISK、HOLD/VERIFY_COST、profitability≤20 |
| `high-sales-loss.json` | 高销量但亏损：MARGIN_RISK、HOLD/VERIFY_COST、profitability≤20、demand≥70 |
| `weak-demand-open-market.json` | supplyGap=NO_STRONG_GAP_SIGNAL、grade∈{C,D}、totalScore<65 |
| `logistics-blocked.json` | BLOCKED_LOGISTICS、logistics=0、BLOCKED/DO_NOT_SAMPLE（raw grade 可计算，机会质量与执行分离） |
| `missing-price.json` | price 维度 N/A、evidenceCoverage=0.9<1 |
| `extreme-outlier.json` | 周转 30325%/评论 157 万/销售额 9.99 亿 → 正常打分，totalScore finite |
| `grade-boundaries.json` | round1 边界对：上方 totalScore≥80 且 grade=A；下方 <80 且 grade≠A（相邻毛利率 21/20） |
| `low-market-context.json` | LOW_MARKET_CONTEXT、暂定评级、supplyGap=null、RESEARCH/COLLECT_MARKET_DATA、coverage=0.75、解释仅"候选池表现"口径、禁"同类市场/市场域" |
| `partial-match.json` | context=LOW 由调用方保证、supplyGap 可计算、解释用"对应 BSR 市场域"、禁"同类市场" |

目的：防止未来 AI 改代码把已冻结规则改坏。固件断言含数值契约（coverage 0.75/0.9、封顶 20）与解释口径契约（三种文案分级）。

---

## 3. 第二层：单调性 / 不变式（7 组 / 20 断言，全绿）

| # | 变更 | 断言 | 实测 |
|---|---|---|---|
| M1 | 销售额 800k→3.5M | demand 不得下降 | 73.1 → 91.5 ✓ |
| M2 | 毛利率 30→50 | profitability 不得下降 | 60 → 85 ✓ |
| M3 | 广告占比 3%→20% | profitability 不得上升 | 95 → 85 ✓ |
| M4 | seller HHI 1200→1800 | competition 不得上升 | 84.5 → 65.5 ✓ |
| M5 | CEL 成本 ×2（渠道不变） | logistics 不得上升 | 79.3 → 64.7 ✓ |
| M6 | 有 CEL → 无 CEL | BLOCKED_LOGISTICS 必须出现、logistics=0、BLOCKED/DO_NOT_SAMPLE | ✓ |
| M7 | 删除 BSR benchmark | competition/price/supplyGap N/A；demand 仍走候选池口径（**不切全局基准**）；LOW_MARKET_CONTEXT 标记 | ✓ |

---

## 4. 第三层：维度验证矩阵（mapped 且可评级 n=229）

Top20% 按各维度分排序（total 按 totalScore），比较组均值/占比 vs baseline：

| 维度 | 验证指标 | baseline | Top20% | 期待 | 判定 |
|---|---|---|---|---|---|
| demand | BSR leader-share | 38.7% | 23.7% | higher | **REVERSED** |
| demand | sales P50（类型级） | 1,593,201 | 663,150 | higher | **REVERSED** |
| competition | seller HHI | 2661.7 | 2507.5 | lower | OK |
| competition | top10 seller share | 87.7% | 75.3% | lower | OK |
| price | 落 P25–P75 比例 | 39.1% | 91.2% | higher | OK |
| profitability | gross margin | 41.5 | 58.0 | higher | OK |
| logistics | shipping ratio | 0.70 | 0.20 | lower | OK |
| operations | sign rate | 77.2 | 91.4 | higher | OK |
| total | Supply Gap HIGH/MEDIUM 占比 | 1.7% | 2.2% | observe | 观察 |
| total | BSR leader-share | 38.7% | 28.6% | record | 记录（0.74×，candidate-weighted；unique-type 52.7%→39.2% 同为 0.74×） |

口径说明：price 落带用**原始 matched type** 的 P25/P75（非 blendBench）；logistics 的 shipping ratio 为逐候选重算（effPrice/RUB-CNY 同引擎口径）。

**结论：8 行中 6 行符合期待方向，2 行 REVERSED（均为 demand），0 行需要紧急修复。** 总分 Top20% 对 leader-share 的 0.74× 按约定只作诊断记录，不作 pass/fail。

---

## 5. demand 两行 REVERSED 的根因（口径语义，非 bug）

demand 维度的公式是**候选 vs 自身市场的分位**（T4-1B §2.2：候选 sales/units/conv/cart_add 与"对应类型/域基准"比较）：

> 候选在**自己所在市场里的相对强度**，而不是"市场需求的绝对规模"。

因此：

- 小市场（sales P50 低）里，中等绝对销售额就能拿到高分位 → demand 高分；
- 大市场（sales P50 高）里，很大的绝对销售额分位反而低 → demand 低分。

按 demand 排序取 Top20%，自然选中"在小市场里占优的候选"：
Top20% 组 matched type 的 sales P50 中位数口径均值 66.3 万 vs baseline 159.3 万；leader-share 23.7% vs 38.7% —— 方向完全由该语义决定。

**这不是代码 bug，也不应通过调权重"修复"**：

- 若业务要验证"市场总需求强的市场"，该信息已在 `marketContext.benchmark.sales_28d.p50` 中，可按类型直接验证，不必借道 demand 维度分；
- 若坚持"demand 分应能富集大市场/领导者市场"，需要改变 demand 的子指标口径（例如混入市场级分位），这属于模型变更，需需求方授权，本阶段未动。

待需求方拍板二选一：**A. 接受现状**（demand=候选相对强度，验证矩阵中 demand 两行改口径或标注为信息项）；**B. 授权口径变更**（T4-4 再动）。

---

## 6. 全量回归（全绿）

| 套件 | 结果 |
|---|---|
| `npm test` | wbEngine 65 + scoring 62 + **monotonicity 20** + **golden 56** + python 31，全绿 |
| `npm run test:golden` | 76/76 |
| `npm run test:sync` | 双端对拍零差异 |
| `npm run test:web-persistence` | 通过 |
| `vite build` | 通过 |

1000 SKU 分布审计（重跑，与 hardening 后一致）：A 1 / B 199 / C 410 / D 199 / **不可评级 191**；LMC 771 行均分 54.7 < mapped 229 行均分 59.1；BLOCKED_LOGISTICS=0。
`mappedResults` 改用 `grade !== null` 后样本 n 不变（229，即此前 mapped 行无 NEEDS_DATA 混入），但样本定义已与冻结契约严格一致。

---

## 7. 文件清单

- 新增：`tests/scoring-golden/{excellent-mapped,poor-negative-margin,high-sales-loss,weak-demand-open-market,logistics-blocked,missing-price,extreme-outlier,grade-boundaries,low-market-context,partial-match}.json`
- 新增：`scripts/run-scoring-golden.js`、`ozon-react/src/utils/scoring/monotonicity.test.mjs`
- 修改：`scripts/t4-score-audit.js`（样本定义修正 + 维度验证矩阵）、`package.json`（测试入口）
- 引擎层（scoringEngine/normalization/explanations/scoring_rules）本轮**零改动**；六维权重未动。
